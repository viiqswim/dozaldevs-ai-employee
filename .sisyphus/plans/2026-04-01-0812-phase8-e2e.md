# Phase 8: Full Local E2E — Work Plan

## TL;DR

> **Quick Summary**: Validate the complete MVP by running the full Jira-webhook-to-PR flow locally, verifying all 12 integration checks pass, and documenting the result. This is the MVP validation milestone — if all 12 checks pass, the system is functionally complete.
>
> **Deliverables**:
>
> - Gateway logging audit (confirm/fix structured logging)
> - Minimal TypeScript GitHub test repo for execution agent
> - Updated seed data pointing to real test repo
> - Realistic Jira webhook test fixture (`jira-realistic-task.json`)
> - Dev startup script (`scripts/dev-start.sh`)
> - Automated 12-point verification script (`scripts/verify-e2e.sh`)
> - Successful full E2E run with all 12 checks passing
> - Updated `.sisyphus/progress.json` with Phase 8 checkpoints
> - Phase 8 completion documentation
>
> **Estimated Effort**: Medium (integration testing, not new feature development)
> **Parallel Execution**: YES — 3 waves + final review wave
> **Critical Path**: T1 → T3 → T4 → T7 → T8 → T9 → T10

---

## Context

### Original Request

Create a granular work plan for the next phase of the AI Employee Platform architecture, ensuring every meaningful implementation task has corresponding automated/manual testing and documentation. Include progress.json updates and a final review phase. Produce a completion doc similar to Phase 1's doc.

### Interview Summary

**Key Discussions**:

- **Test repo**: User chose to create a minimal TypeScript test repo (purpose-built for E2E testing, simpler/faster)
- **12-point verification**: User chose automated script (`verify-e2e.sh`) with pass/fail reporting — reusable for Phase 9
- **Startup automation**: User chose a dev startup script (`scripts/dev-start.sh`) that launches all services

**Research Findings**:

- Seed data uses project key `TEST` and placeholder repo URL `https://github.com/your-org/your-test-repo` — must be updated to real repo
- Existing test fixtures in `test-payloads/` already cover Jira webhook formats — only a "realistic task" fixture needed
- `scripts/verify-phase1.sh` (168 lines) provides the exact PASS/FAIL pattern to follow for `verify-e2e.sh`
- 503 tests passing at Phase 7 baseline — must not regress
- Gateway logging migration (Phase 7 known limitation #3) may be a non-issue — audit first

### Metis Review

**Identified Gaps** (addressed):

- **Gateway logging may not need migration**: Metis found gateway files may already use Fastify's request.log. Plan starts with audit task that cancels migration if no `console.log` found
- **Test fixture project key**: Must match seeded `TEST` key in `prisma/seed.ts`. Addressed in Task 4
- **GitHub test repo name**: External dependency — must be decided before E2E run. Flagged as decision needed
- **Stale DB between runs**: `dev-start.sh` should support optional DB reset. Addressed in Task 5
- **Container cleanup race**: `verify-e2e.sh` needs a poll/wait for container cleanup check. Addressed in Task 6
- **Inngest dashboard check is manual**: Check #2 cannot be fully automated — explicitly flagged in Task 7

---

## Work Objectives

### Core Objective

Validate the complete MVP by running the full Jira-webhook-to-PR flow entirely locally, proving that all components from Phases 1-7 work together end-to-end.

### Concrete Deliverables

- `scripts/dev-start.sh` — launches all local services
- `scripts/verify-e2e.sh` — automated 12-point checklist verification
- `test-payloads/jira-realistic-task.json` — realistic task fixture
- GitHub test repo (`viiqswim/ai-employee-test-target`) — minimal TypeScript project
- Updated `prisma/seed.ts` — pointing to real test repo
- Updated `.sisyphus/progress.json` — all Phase 8 checkpoints complete
- `docs/YYYY-MM-DD-HHMM-phase8-e2e.md` — completion documentation

### Definition of Done

- [ ] All 12 E2E verification checks pass (`scripts/verify-e2e.sh` exits 0)
- [ ] 503+ tests still passing (`pnpm test -- --run`)
- [ ] All 7 Phase 8 checkpoints in progress.json marked "complete"
- [ ] Phase 8 completion doc written and committed

### Must Have

- Gateway logging confirmed as structured (audit or fix)
- Realistic Jira webhook fixture using project key `TEST`
- Full webhook-to-PR flow completing locally
- Task status audit trail: NULL→Ready(gateway)→Executing(lifecycle_fn)→Submitting(machine)→Done(lifecycle_fn)
- All database records populated: tasks, executions, validation_runs, deliverables, task_status_log
- Worker container self-destructs after completion
- Automated verification script following `verify-phase1.sh` pattern

### Must NOT Have (Guardrails)

- MUST NOT add new Vitest unit tests (Phase 8 is integration testing, not unit testing)
- MUST NOT modify any files in `src/gateway/`, `src/inngest/`, `src/lib/`, `src/workers/` beyond the gateway logging fix (if needed)
- MUST NOT refactor any Phase 1-7 source code
- MUST NOT add GitHub Actions, CI configuration, or deployment configs
- MUST NOT modify `prisma/schema.prisma` or create new migrations
- MUST NOT update Phase 1-7 documentation files
- MUST NOT add `docker-compose.yml` or container orchestration beyond the startup script
- MUST NOT add pino-pretty or new logging infrastructure
- MUST NOT create a test repo with CI, Actions, Husky, badges, or anything beyond 5-6 core files

---

## Verification Strategy (MANDATORY)

> **ZERO HUMAN INTERVENTION** — ALL verification is agent-executed. No exceptions.
> The ONE exception: Inngest Dev dashboard visual check (#2) — explicitly flagged as manual in Task 7.

### Test Decision

- **Infrastructure exists**: YES (Vitest, 503 tests)
- **Automated tests**: None new — Phase 8 verifies via bash scripts and E2E flow, not unit tests
- **Framework**: Vitest (existing — regression check only), bash scripts (new verification)
- **Regression guard**: `pnpm test -- --run` must still pass 503+ tests after every commit

### QA Policy

Every task includes agent-executed QA scenarios. Evidence saved to `.sisyphus/evidence/task-{N}-{scenario-slug}.{ext}`.

- **Scripts**: Use Bash — run script, check exit code, validate output
- **Fixtures**: Use Bash (curl) — send payload to gateway, assert response
- **E2E flow**: Use Bash (curl + psql + gh) — follow 12-point checklist verbatim
- **Gateway audit**: Use `ast_grep_search` — confirm zero `console.log` matches

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Start Immediately — audit + foundation):
├── Task 1: Gateway logging audit + fix [quick]
├── Task 2: Create minimal GitHub test repo [quick]
└── Task 3: Update seed data with real repo URL [quick]

Wave 2 (After Wave 1 — fixtures + scripts):
├── Task 4: Create realistic Jira webhook fixture (depends: 3) [quick]
├── Task 5: Create dev startup script (depends: none from Wave 1) [unspecified-high]
└── Task 6: Create E2E verification script (depends: none from Wave 1) [unspecified-high]

Wave 3 (After Wave 2 — E2E execution + verification):
├── Task 7: Run full E2E flow (depends: 2, 3, 4, 5) [deep]
└── Task 8: Run automated verification + capture evidence (depends: 6, 7) [unspecified-high]

Wave 4 (After Wave 3 — documentation + progress):
├── Task 9: Update progress.json (depends: 8) [quick]
└── Task 10: Write Phase 8 completion documentation (depends: 8, 9) [writing]

Wave FINAL (After ALL tasks — 4 parallel reviews, then user okay):
├── Task F1: Plan compliance audit (oracle)
├── Task F2: Code quality review (unspecified-high)
├── Task F3: Real manual QA (unspecified-high)
└── Task F4: Scope fidelity check (deep)
-> Present results -> Get explicit user okay

Critical Path: T1 → T3 → T4 → T7 → T8 → T9 → T10 → F1-F4 → user okay
Parallel Speedup: ~50% faster than sequential
Max Concurrent: 3 (Wave 1 & Wave 2)
```

### Dependency Matrix

| Task | Depends On         | Blocks                             |
| ---- | ------------------ | ---------------------------------- |
| T1   | —                  | T7 (must confirm gateway is clean) |
| T2   | —                  | T3, T7                             |
| T3   | T2                 | T4, T7                             |
| T4   | T3                 | T7                                 |
| T5   | —                  | T7                                 |
| T6   | —                  | T8                                 |
| T7   | T1, T2, T3, T4, T5 | T8                                 |
| T8   | T6, T7             | T9, T10                            |
| T9   | T8                 | T10                                |
| T10  | T8, T9             | F1-F4                              |

### Agent Dispatch Summary

- **Wave 1**: **3** — T1 → `quick`, T2 → `quick`, T3 → `quick`
- **Wave 2**: **3** — T4 → `quick`, T5 → `unspecified-high`, T6 → `unspecified-high`
- **Wave 3**: **2** — T7 → `deep`, T8 → `unspecified-high`
- **Wave 4**: **2** — T9 → `quick`, T10 → `writing`
- **FINAL**: **4** — F1 → `oracle`, F2 → `unspecified-high`, F3 → `unspecified-high`, F4 → `deep`

---

## TODOs

> Implementation + Verification = ONE Task. Never separate.
> EVERY task MUST have: Recommended Agent Profile + Parallelization info + QA Scenarios.
> **A task WITHOUT QA Scenarios is INCOMPLETE. No exceptions.**

- [x] 1. Gateway Logging Audit + Fix

  **What to do**:
  - Run `ast_grep_search` with pattern `console.log($$$)` in `src/gateway/` to find all `console.log` calls
  - Also run `grep -r "console.warn\|console.error" src/gateway/` to catch all console methods
  - If **zero matches found**: Update Phase 7 doc known limitations section to remove item #3 (deferred gateway logging). Task is complete.
  - If **matches found**: Replace each `console.log`/`console.warn`/`console.error` with the structured pino logger using `createLogger('gateway')` from `src/lib/logger.ts`. Follow the exact pattern used in `src/inngest/lifecycle.ts` and `src/workers/orchestrate.mts`.
  - Run `pnpm test -- --run` to confirm 503+ tests still pass after any changes

  **Must NOT do**:
  - MUST NOT refactor gateway route handlers or business logic
  - MUST NOT add pino-pretty or any new dependencies
  - MUST NOT modify test files
  - MUST NOT change gateway behavior — only logging output format

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: This is a small audit task — either no work (confirm clean) or a trivial find-and-replace
  - **Skills**: []
    - No specialized skills needed — standard code search and edit

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 2, 3)
  - **Blocks**: Task 7 (must confirm gateway is clean before E2E run)
  - **Blocked By**: None (can start immediately)

  **References**:

  **Pattern References**:
  - `src/lib/logger.ts` — The structured pino logger factory. Use `createLogger('gateway')` for gateway-scoped logging.
  - `src/inngest/lifecycle.ts` — Example of structured logger usage in Inngest functions (Phase 7 migration pattern)
  - `src/workers/orchestrate.mts` — Example of structured logger usage in worker code (Phase 7 migration pattern)

  **API/Type References**:
  - `src/lib/logger.ts:createLogger` — Returns a pino.Logger child with `component` bound
  - `src/lib/logger.ts:taskLogger` — Returns a logger with both `component` and `taskId` bound

  **Context References**:
  - `docs/2026-04-01-0114-phase7-resilience.md` lines 443-444 — Phase 7 known limitation #3: "Gateway logging deferred: console.log calls in src/gateway/ are NOT migrated to the structured logger"
  - `src/gateway/server.ts` — Main gateway server file to audit
  - `src/gateway/routes/` — Route handlers to audit
  - `src/gateway/services/` — Service files to audit

  **WHY Each Reference Matters**:
  - `logger.ts` provides the exact API to use if any console.log calls are found
  - Phase 7 lifecycle/orchestrate files show the established migration pattern (import logger, replace console calls)
  - Phase 7 doc is what needs updating if the audit finds zero console.log calls

  **Acceptance Criteria**:
  - [ ] `ast_grep_search` with `console.log($$$)` in `src/gateway/` returns zero matches
  - [ ] `grep -rn "console\.\(log\|warn\|error\)" src/gateway/` returns zero matches (excluding .gitkeep)
  - [ ] `pnpm test -- --run` passes 503+ tests, 0 failures
  - [ ] Either: Phase 7 doc limitation #3 updated, OR: console calls replaced with structured logger

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Gateway has zero console.log calls (expected happy path)
    Tool: Bash
    Preconditions: Codebase is at Phase 7 baseline
    Steps:
      1. Run: ast_grep_search pattern="console.log($$$)" lang="typescript" paths=["src/gateway/"]
      2. Assert: zero matches returned
      3. Run: grep -rn "console\.\(log\|warn\|error\)" src/gateway/ --include="*.ts" | wc -l
      4. Assert: output is "0"
    Expected Result: Zero console.log/warn/error calls in any gateway TypeScript file
    Failure Indicators: Any match found means migration work is needed
    Evidence: .sisyphus/evidence/task-1-gateway-audit.txt

  Scenario: Test suite unchanged after audit
    Tool: Bash
    Preconditions: Audit complete (no changes or logging fix applied)
    Steps:
      1. Run: pnpm test -- --run 2>&1 | tail -5
      2. Assert: output contains "503" or higher test count and "0 failed"
    Expected Result: 503+ tests pass, 0 failures — no regression
    Failure Indicators: Any test failure or test count drop below 503
    Evidence: .sisyphus/evidence/task-1-test-regression.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-1-gateway-audit.txt` — full grep/ast_grep output
  - [ ] `task-1-test-regression.txt` — test run summary

  **Commit**: YES
  - Message: `chore(gateway): confirm structured logging — no console.log migration needed`
  - Files: `docs/2026-04-01-0114-phase7-resilience.md` (update known limitations if applicable)
  - Pre-commit: `pnpm test -- --run`

- [x] 2. Create Minimal GitHub Test Repo

  **What to do**:
  - Create a minimal TypeScript project on GitHub named `viiqswim/ai-employee-test-target`
  - The repo must be a **real, minimal TypeScript project** with a working test suite that the execution agent can validate against
  - Files to create (EXACTLY these, nothing more):
    - `package.json` — pnpm, ESM, Node >=20, with `test`, `build`, `lint` scripts
    - `tsconfig.json` — strict TypeScript
    - `src/index.ts` — exports a single utility function (e.g., `formatDate(date: Date): string`)
    - `src/index.test.ts` — 2-3 simple Vitest tests for the utility function
    - `.gitignore` — node_modules, dist
    - `README.md` — one-line description
  - Push to GitHub with `gh repo create`
  - Verify the repo is accessible and clone-able

  **Must NOT do**:
  - MUST NOT add ESLint, Prettier, Husky, GitHub Actions, badges, CI configs
  - MUST NOT add more than one source file beyond `src/index.ts`
  - MUST NOT add Docker, Supabase, or any complex dependencies
  - MUST NOT spend time on README content or documentation

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Creating 6 files and pushing to GitHub is a trivial task
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1, 3)
  - **Blocks**: Task 3 (needs repo URL), Task 7 (execution agent targets this repo)
  - **Blocked By**: None (can start immediately)

  **References**:

  **Pattern References**:
  - `package.json` (root project) — Reference for pnpm + ESM + Node >=20 configuration pattern
  - `tsconfig.json` (root project) — Reference for strict TypeScript config

  **External References**:
  - The test repo must use `pnpm` (matching the worker Docker image which has pnpm installed per `Dockerfile`)
  - Tests must use Vitest (matching the project's test runner)

  **WHY Each Reference Matters**:
  - The execution agent runs `pnpm install`, `pnpm build`, `pnpm test` inside the Docker container — the test repo's scripts must match
  - If the test repo uses `npm` or `bun`, the validation pipeline will fail

  **Acceptance Criteria**:
  - [ ] `gh repo view viiqswim/ai-employee-test-target --json name,url` returns HTTP 200
  - [ ] `git clone [repo-url] /tmp/test-target && cd /tmp/test-target && pnpm install && pnpm build && pnpm test` succeeds
  - [ ] Test repo has exactly 6 files (no more)

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Test repo is accessible and functional
    Tool: Bash
    Preconditions: GitHub token available, repo creation complete
    Steps:
      1. Run: gh repo view viiqswim/ai-employee-test-target --json name,url,isPrivate
      2. Assert: name matches, isPrivate is false (public repo)
      3. Run: git clone [repo-url] /tmp/e2e-test-target
      4. Run: cd /tmp/e2e-test-target && pnpm install --frozen-lockfile
      5. Run: pnpm build
      6. Assert: exit code 0 (TypeScript compiles)
      7. Run: pnpm test
      8. Assert: exit code 0, 2+ tests pass
    Expected Result: Repo exists, clones, installs, builds, and tests pass
    Failure Indicators: Any step exits non-zero
    Evidence: .sisyphus/evidence/task-2-test-repo-verify.txt

  Scenario: Test repo has minimal file count
    Tool: Bash
    Preconditions: Repo cloned to /tmp/e2e-test-target
    Steps:
      1. Run: find /tmp/e2e-test-target -maxdepth 2 -type f | grep -v node_modules | grep -v .git | wc -l
      2. Assert: output is 6 or 7 (6 files + possibly pnpm-lock.yaml)
    Expected Result: No extraneous files (no CI, no Husky, no ESLint config)
    Failure Indicators: File count > 8
    Evidence: .sisyphus/evidence/task-2-file-count.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-2-test-repo-verify.txt` — clone + build + test output
  - [ ] `task-2-file-count.txt` — file listing

  **Commit**: YES (groups with T3)
  - Message: `feat: create minimal test repo for e2e execution target`
  - Files: External GitHub repo (no local files)
  - Pre-commit: `gh repo view viiqswim/ai-employee-test-target`

- [x] 3. Update Seed Data with Real Test Repo URL

  **What to do**:
  - Edit `prisma/seed.ts` to replace placeholder `repo_url: 'https://github.com/your-org/your-test-repo'` with the real test repo URL from Task 2
  - Keep `jira_project_key: 'TEST'` unchanged (fixtures use this key)
  - Keep all other seed data unchanged (project UUID `00000000-0000-0000-0000-000000000003`, agent version UUID `00000000-0000-0000-0000-000000000002`)
  - Run `pnpm prisma db seed` to apply the update
  - Verify the project record in the database has the new URL

  **Must NOT do**:
  - MUST NOT change the project UUID or name
  - MUST NOT change the agent version seed data
  - MUST NOT add new seed records
  - MUST NOT modify the schema or run migrations

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Single-line edit in seed.ts + re-seed
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: NO (must wait for T2 to know the repo URL)
  - **Parallel Group**: Wave 1 (sequential after T2)
  - **Blocks**: Task 4, Task 7
  - **Blocked By**: Task 2

  **References**:

  **Pattern References**:
  - `prisma/seed.ts:29-44` — The project upsert that needs the repo_url updated
  - `prisma/seed.ts:37` — The `jira_project_key: 'TEST'` field that must remain unchanged

  **WHY Each Reference Matters**:
  - The seed's `repo_url` is read by the lifecycle function to pass `REPO_URL` to the Fly.io machine environment
  - The `jira_project_key: 'TEST'` must match the test fixture's project key for gateway filtering to work

  **Acceptance Criteria**:
  - [ ] `prisma/seed.ts` contains the real GitHub test repo URL (not `your-org/your-test-repo`)
  - [ ] `pnpm prisma db seed` runs without error
  - [ ] `psql postgresql://postgres:postgres@localhost:54322/ai_employee -c "SELECT repo_url FROM projects WHERE id = '00000000-0000-0000-0000-000000000003';"` returns the real repo URL

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Seed data updated with real repo URL
    Tool: Bash
    Preconditions: Local Supabase running, seed.ts edited with real URL
    Steps:
      1. Run: pnpm prisma db seed
      2. Assert: exit code 0, output contains "Project upserted"
      3. Run: psql postgresql://postgres:postgres@localhost:54322/ai_employee -t -c "SELECT repo_url FROM projects WHERE id = '00000000-0000-0000-0000-000000000003';"
      4. Assert: output contains the real test repo URL (not 'your-org/your-test-repo')
    Expected Result: Project record in DB has the real GitHub repo URL
    Failure Indicators: Still shows placeholder URL or seed fails
    Evidence: .sisyphus/evidence/task-3-seed-verify.txt

  Scenario: Jira project key unchanged
    Tool: Bash
    Preconditions: Seed applied
    Steps:
      1. Run: psql postgresql://postgres:postgres@localhost:54322/ai_employee -t -c "SELECT jira_project_key FROM projects WHERE id = '00000000-0000-0000-0000-000000000003';"
      2. Assert: output is "TEST"
    Expected Result: Project key still "TEST" for fixture compatibility
    Failure Indicators: Project key changed from TEST
    Evidence: .sisyphus/evidence/task-3-project-key.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-3-seed-verify.txt` — seed output + DB query result
  - [ ] `task-3-project-key.txt` — project key confirmation

  **Commit**: YES (groups with T2)
  - Message: `feat: update seed data with real test repo URL`
  - Files: `prisma/seed.ts`
  - Pre-commit: `pnpm prisma db seed`

- [x] 4. Create Realistic Jira Webhook Test Fixture

  **What to do**:
  - Create `test-payloads/jira-realistic-task.json` — a realistic Jira webhook payload that describes a **simple, achievable task** for the test repo
  - The task should be something like: "Add a utility function that formats a number as USD currency (e.g., 1234.5 → '$1,234.50')"
  - The payload MUST use project key `TEST` to match the seeded project's `jira_project_key`
  - The payload MUST have all required fields per the existing Zod schema: `webhookEvent`, `issue.id`, `issue.key`, `issue.fields.summary`, `issue.fields.project.key`
  - Reference existing fixture `test-payloads/jira-issue-created.json` for the exact JSON structure
  - The `webhookEvent` must be `jira:issue_created`
  - The `issue.key` should be `TEST-100` (distinct from any existing fixtures)
  - Include a meaningful `description` field with clear acceptance criteria that the execution agent can verify

  **Must NOT do**:
  - MUST NOT modify existing fixtures in `test-payloads/`
  - MUST NOT use a project key other than `TEST`
  - MUST NOT create a task that requires external dependencies, database changes, or complex setup

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Creating a single JSON file following an established pattern
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T5, T6 in Wave 2)
  - **Parallel Group**: Wave 2
  - **Blocks**: Task 7 (the E2E run uses this fixture)
  - **Blocked By**: Task 3 (need confirmed project key)

  **References**:

  **Pattern References**:
  - `test-payloads/jira-issue-created.json` — Existing fixture showing exact JSON structure. Copy this and modify for a realistic task.
  - `src/gateway/validation/jira.ts` (or equivalent Zod schema file) — The Zod schema that validates Jira payloads. The fixture MUST pass this validation.

  **API/Type References**:
  - The implementation phases doc Phase 8 section (line 925) references `test-payloads/jira-realistic-task.json` — this is the exact filename to use

  **WHY Each Reference Matters**:
  - The existing fixture provides the exact JSON shape; copying it avoids Zod validation failures
  - The project key `TEST` must match the seeded project for gateway project filtering to pass

  **Acceptance Criteria**:
  - [ ] File `test-payloads/jira-realistic-task.json` exists
  - [ ] Payload uses `webhookEvent: "jira:issue_created"` and `issue.fields.project.key: "TEST"`
  - [ ] Payload includes clear, simple task description with acceptance criteria
  - [ ] Existing tests still pass: `pnpm test -- --run` 503+ tests

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Fixture validates against gateway
    Tool: Bash
    Preconditions: Gateway running on localhost:3000, fixture created
    Steps:
      1. Compute valid HMAC signature for the fixture body using JIRA_WEBHOOK_SECRET
      2. Run: curl -s -o /tmp/response.json -w "%{http_code}" -X POST http://localhost:3000/webhooks/jira \
           -H "Content-Type: application/json" \
           -H "X-Hub-Signature: sha256=<computed-hmac>" \
           -d @test-payloads/jira-realistic-task.json
      3. Assert: HTTP status is 200 or 202 (not 400 or 401)
    Expected Result: Gateway accepts the fixture and creates a task record
    Failure Indicators: 400 (validation fail) or 401 (signature fail)
    Evidence: .sisyphus/evidence/task-4-fixture-validation.txt

  Scenario: Fixture has all required fields
    Tool: Bash
    Preconditions: Fixture file exists
    Steps:
      1. Run: cat test-payloads/jira-realistic-task.json | jq '.webhookEvent'
      2. Assert: output is "jira:issue_created"
      3. Run: cat test-payloads/jira-realistic-task.json | jq '.issue.fields.project.key'
      4. Assert: output is "TEST"
      5. Run: cat test-payloads/jira-realistic-task.json | jq '.issue.key'
      6. Assert: output is "TEST-100"
    Expected Result: All required fields present with correct values
    Failure Indicators: Any field missing or wrong value
    Evidence: .sisyphus/evidence/task-4-fixture-fields.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-4-fixture-validation.txt` — curl response
  - [ ] `task-4-fixture-fields.txt` — jq field extraction

  **Commit**: YES
  - Message: `test: add realistic jira webhook fixture for e2e testing`
  - Files: `test-payloads/jira-realistic-task.json`
  - Pre-commit: `pnpm test -- --run`

- [x] 5. Create Dev Startup Script

  **What to do**:
  - Create `scripts/dev-start.sh` — a script that launches all required services for the local E2E test
  - The script must start (in order): Local Supabase, Inngest Dev Server, Event Gateway
  - Each service must have a health check wait before proceeding to the next
  - The script must accept an optional `--reset` flag that cleans the database before starting (DELETE FROM task_status_log, tasks, executions, etc.)
  - Must trap `SIGINT`/`SIGTERM` to cleanly stop all background processes on `Ctrl+C`
  - Must check prerequisites before starting: Docker Desktop running, Supabase CLI available, required env vars set
  - Must print a summary banner showing all service URLs when ready
  - Follow the pattern of `scripts/verify-phase1.sh` for formatting (the `check_pass`/`check_fail` pattern)

  **Must NOT do**:
  - MUST NOT create a docker-compose.yml
  - MUST NOT add service discovery or log aggregation
  - MUST NOT install new dependencies
  - MUST NOT modify any existing source files
  - MUST NOT hard-code credentials — read from `.env` or environment

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Multi-step bash script with health checks, traps, and error handling requires careful implementation
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T4, T6 in Wave 2)
  - **Parallel Group**: Wave 2
  - **Blocks**: Task 7 (services must be running for E2E)
  - **Blocked By**: None from Wave 1 (scripts can be written before seed update)

  **References**:

  **Pattern References**:
  - `scripts/verify-phase1.sh:1-25` — PASS/FAIL formatting pattern, `set -o pipefail`, header banner style
  - `docs/2026-03-25-1901-mvp-implementation-phases.md:892-914` — The exact services to start and their environment variables for Phase 8 setup

  **External References**:
  - The implementation phases doc specifies these terminals/services:
    - Terminal 1: `supabase start` + `pnpm prisma migrate dev`
    - Terminal 2: `npx inngest-cli@latest dev` (port 8288)
    - Terminal 3: Event Gateway with specific env vars (port 3000)

  **WHY Each Reference Matters**:
  - `verify-phase1.sh` establishes the formatting convention for all scripts in this project
  - The implementation phases doc has the exact env vars and commands — must match verbatim

  **Acceptance Criteria**:
  - [ ] `scripts/dev-start.sh` exists and is executable (`chmod +x`)
  - [ ] `bash -n scripts/dev-start.sh` passes (no syntax errors)
  - [ ] Running the script starts all 3 services and reports healthy status
  - [ ] `curl -sf http://localhost:3000/health` returns 200 within 60 seconds of script start
  - [ ] `Ctrl+C` cleanly stops all background processes (no orphan processes)
  - [ ] `--reset` flag cleans the database tables

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: All services start and health checks pass
    Tool: Bash
    Preconditions: Docker Desktop running, .env configured, Supabase already initialized
    Steps:
      1. Run: bash scripts/dev-start.sh &
      2. Wait: up to 60 seconds for script to print "All services healthy"
      3. Run: curl -sf http://localhost:3000/health
      4. Assert: response contains "ok" or status 200
      5. Run: curl -sf http://localhost:8288/
      6. Assert: Inngest Dev Server responding (status 200)
    Expected Result: Gateway health check passes, Inngest responds, banner shows all URLs
    Failure Indicators: Script exits early, health check times out, or gateway returns non-200
    Evidence: .sisyphus/evidence/task-5-services-healthy.txt

  Scenario: Script fails fast on missing prerequisites
    Tool: Bash
    Preconditions: JIRA_WEBHOOK_SECRET unset
    Steps:
      1. Run: unset JIRA_WEBHOOK_SECRET && bash scripts/dev-start.sh 2>&1
      2. Assert: exit code is non-zero
      3. Assert: output mentions missing environment variable
    Expected Result: Script exits with clear error message about missing prereqs
    Failure Indicators: Script starts without required env vars
    Evidence: .sisyphus/evidence/task-5-prereq-fail.txt

  Scenario: Clean shutdown on Ctrl+C
    Tool: interactive_bash (tmux)
    Preconditions: dev-start.sh running
    Steps:
      1. Run: bash scripts/dev-start.sh in a tmux session
      2. Wait for "All services healthy" message
      3. Send: Ctrl+C
      4. Wait 5 seconds
      5. Run: ps aux | grep -E "(inngest|fastify|node.*gateway)" | grep -v grep | wc -l
      6. Assert: output is "0" (no orphan processes)
    Expected Result: All child processes terminated cleanly
    Failure Indicators: Orphan processes remain after Ctrl+C
    Evidence: .sisyphus/evidence/task-5-clean-shutdown.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-5-services-healthy.txt` — service startup output + health check responses
  - [ ] `task-5-prereq-fail.txt` — prerequisite failure output
  - [ ] `task-5-clean-shutdown.txt` — process list after Ctrl+C

  **Commit**: YES (groups with T6)
  - Message: `feat: add dev startup script for local e2e environment`
  - Files: `scripts/dev-start.sh`
  - Pre-commit: `bash -n scripts/dev-start.sh`

- [x] 6. Create E2E Verification Script

  **What to do**:
  - Create `scripts/verify-e2e.sh` — implements the exact 12-point checklist from `docs/2026-03-25-1901-mvp-implementation-phases.md` lines 935-993
  - Follow the `scripts/verify-phase1.sh` structure exactly: `set -o pipefail`, PASS/FAIL counters, banner header, numbered checks
  - The script must accept a `--task-id` argument (the UUID of the task to verify) OR auto-detect the most recent task
  - Must also accept `--repo` argument for the GitHub test repo name
  - Each of the 12 checks must map exactly to the implementation phases doc:
    1. Task created in Supabase (status check)
    2. Event visible in Inngest Dev dashboard (**MANUAL — print URL and instruction, skip automated check**)
    3. Lifecycle function triggered, status = Executing
    4. Docker container boots (docker ps)
    5. Heartbeats appearing (executions.heartbeat_at)
    6. Validation runs recorded (validation_runs table)
    7. PR created on GitHub (gh pr list)
    8. Task status = Done
    9. Full status log audit trail (4 transitions)
    10. Deliverable record exists
    11. Execution record fully populated
    12. Container cleaned up (docker ps, **with 30-second poll/wait**)
  - For check #12 (container cleanup), use a poll loop with 5-second intervals up to 30 seconds — the container may still be stopping
  - Print final summary: `X/12 checks passed`
  - Exit 0 if all pass, exit 1 if any fail

  **Must NOT do**:
  - MUST NOT implement retry loops for checks (except #12 container cleanup poll)
  - MUST NOT add HTML report generation
  - MUST NOT use `<your-test-repo>` placeholder — use the `--repo` argument
  - MUST NOT add parallel check execution
  - MUST NOT modify any source files

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: 12 distinct verification checks with SQL queries, GitHub API calls, and docker commands
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T4, T5 in Wave 2)
  - **Parallel Group**: Wave 2
  - **Blocks**: Task 8 (verification run uses this script)
  - **Blocked By**: None from Wave 1

  **References**:

  **Pattern References**:
  - `scripts/verify-phase1.sh` — EXACT structure to follow. Copy the `check_pass`/`check_fail` functions, banner format, and summary pattern.
  - `docs/2026-03-25-1901-mvp-implementation-phases.md:935-993` — The authoritative 12-point checklist. Implement these EXACT commands. Do not paraphrase.

  **API/Type References**:
  - SQL queries must use `psql postgresql://postgres:postgres@localhost:54322/ai_employee` to target the correct database
  - GitHub checks use `gh pr list --repo <repo> --head "ai/<ticket-id>-*"`
  - Docker checks use `docker ps --filter "ancestor=ai-employee-worker"`

  **WHY Each Reference Matters**:
  - `verify-phase1.sh` is the project's script convention — deviating creates inconsistency
  - The 12-point checklist is the AUTHORITATIVE acceptance gate for the entire MVP — implementing wrong checks defeats the purpose

  **Acceptance Criteria**:
  - [ ] `scripts/verify-e2e.sh` exists and is executable
  - [ ] `bash -n scripts/verify-e2e.sh` passes (no syntax errors)
  - [ ] Script implements all 12 checks from the implementation phases doc
  - [ ] Check #2 is explicitly marked as manual with instructions printed
  - [ ] Check #12 uses poll/wait for container cleanup (not instant assertion)
  - [ ] Script accepts `--task-id` and `--repo` arguments
  - [ ] Exit code is 0 when all checks pass, 1 when any fail

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Script has correct structure and syntax
    Tool: Bash
    Preconditions: Script file exists
    Steps:
      1. Run: bash -n scripts/verify-e2e.sh
      2. Assert: exit code 0 (no syntax errors)
      3. Run: head -5 scripts/verify-e2e.sh
      4. Assert: starts with #!/bin/bash and set -o pipefail
      5. Run: grep -c "check_pass\|check_fail" scripts/verify-e2e.sh
      6. Assert: count >= 24 (at least 2 per check — pass and fail paths)
    Expected Result: Script follows verify-phase1.sh structure
    Failure Indicators: Syntax error, missing shebang, or missing check functions
    Evidence: .sisyphus/evidence/task-6-script-structure.txt

  Scenario: Script reports failures correctly against unhealthy environment
    Tool: Bash
    Preconditions: No services running (gateway down)
    Steps:
      1. Run: bash scripts/verify-e2e.sh --task-id "00000000-0000-0000-0000-000000000000" --repo "test/repo" 2>&1
      2. Assert: exit code is 1
      3. Assert: output contains at least one "FAIL"
      4. Assert: output contains "X/12 checks passed" where X < 12
    Expected Result: Script correctly reports failures when environment is down
    Failure Indicators: Script exits 0 despite no services running
    Evidence: .sisyphus/evidence/task-6-failure-mode.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-6-script-structure.txt` — syntax check + structure analysis
  - [ ] `task-6-failure-mode.txt` — failure mode output

  **Commit**: YES (groups with T5)
  - Message: `feat: add automated e2e verification script for 12-point checklist`
  - Files: `scripts/verify-e2e.sh`
  - Pre-commit: `bash -n scripts/verify-e2e.sh`

- [x] 7. Run Full E2E Flow

  **What to do**:
  - This is the **critical integration test** — running the complete Jira-webhook-to-PR flow locally
  - Prerequisites: All services running via `scripts/dev-start.sh`, database seeded, test repo exists on GitHub
  - Clean the database first: `scripts/dev-start.sh --reset` or manually DELETE FROM task_status_log, validation_runs, deliverables, executions, tasks
  - Compute a valid HMAC signature for `test-payloads/jira-realistic-task.json` using the `JIRA_WEBHOOK_SECRET`
  - Send the webhook: `curl -s -X POST http://localhost:3000/webhooks/jira -H "Content-Type: application/json" -H "X-Hub-Signature: sha256=<hmac>" -d @test-payloads/jira-realistic-task.json`
  - Monitor the flow in real-time:
    - Check Inngest Dev dashboard at `http://localhost:8288` for function execution (**this is the one manual check**)
    - Poll task status: `psql -c "SELECT status FROM tasks ORDER BY created_at DESC LIMIT 1;"`
    - Watch Docker containers: `docker ps --filter "ancestor=ai-employee-worker"`
    - Monitor heartbeats: `psql -c "SELECT heartbeat_at, current_stage FROM executions ORDER BY created_at DESC LIMIT 1;"`
  - Wait for the full flow to complete (expected: 5-15 minutes depending on task complexity and LLM latency)
  - Task should reach status `Done` with a PR created on GitHub
  - Record the task UUID for use in Task 8

  **Must NOT do**:
  - MUST NOT modify any source code to make the flow work — if it fails, that's a Phase 1-7 bug to fix separately
  - MUST NOT skip the Inngest dashboard manual check (check #2)
  - MUST NOT use a mock/simulated flow — this must exercise the real Docker container, real OpenCode, real GitHub

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: This is the MVP validation run — requires careful monitoring, real-time debugging if issues arise, and patience for the 5-15 minute flow
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: NO (sequential — this is THE test)
  - **Parallel Group**: Wave 3
  - **Blocks**: Task 8 (verification needs the completed task)
  - **Blocked By**: Tasks 1, 2, 3, 4, 5

  **References**:

  **Pattern References**:
  - `docs/2026-03-25-1901-mvp-implementation-phases.md:888-993` — The FULL Phase 8 test procedure. Follow this EXACTLY.
  - `docs/2026-03-22-2317-ai-employee-architecture.md` §11 — Full lifecycle sequence (steps 1-20 for MVP)

  **External References**:
  - The implementation phases doc specifies: "This is the integration test that proves the MVP works. Follow these steps in order."

  **WHY Each Reference Matters**:
  - The test procedure is the authoritative specification — deviating means testing the wrong thing
  - §11 shows the expected sequence of events to watch for during the flow

  **Acceptance Criteria**:
  - [ ] Webhook sent and accepted (HTTP 200/202 response)
  - [ ] Task record created in Supabase with status `Ready`
  - [ ] Inngest Dev dashboard shows `engineering/task-lifecycle` function running (MANUAL CHECK)
  - [ ] Docker container `ai-employee-worker` visible in `docker ps`
  - [ ] Heartbeats written to `executions.heartbeat_at`
  - [ ] Validation runs recorded in `validation_runs` table
  - [ ] PR created on GitHub with title format `[AI] TEST-100: <summary>`
  - [ ] Task status reaches `Done`
  - [ ] Status log shows: NULL→Ready(gateway), Ready→Executing(lifecycle_fn), Executing→Submitting(machine), Submitting→Done(lifecycle_fn)
  - [ ] Container self-destructs (no longer in `docker ps`)

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Full webhook-to-PR flow completes successfully
    Tool: Bash + interactive_bash (tmux for monitoring)
    Preconditions: All services running (dev-start.sh), DB clean, test repo exists
    Steps:
      1. Clean DB: psql -c "DELETE FROM task_status_log; DELETE FROM validation_runs; DELETE FROM deliverables; DELETE FROM executions; DELETE FROM tasks;"
      2. Compute HMAC: echo -n "$(cat test-payloads/jira-realistic-task.json)" | openssl dgst -sha256 -hmac "$JIRA_WEBHOOK_SECRET" | awk '{print $2}'
      3. Send webhook: curl -s -w "\n%{http_code}" -X POST http://localhost:3000/webhooks/jira \
           -H "Content-Type: application/json" \
           -H "X-Hub-Signature: sha256=<hmac>" \
           -d @test-payloads/jira-realistic-task.json
      4. Assert: HTTP status 200 or 202
      5. Record task ID: psql -t -c "SELECT id FROM tasks ORDER BY created_at DESC LIMIT 1;"
      6. Poll status every 30 seconds for up to 20 minutes: psql -t -c "SELECT status FROM tasks WHERE id = '<task-id>';"
      7. Assert: status eventually reaches "Done"
      8. Verify PR: gh pr list --repo viiqswim/ai-employee-test-target --head "ai/TEST-100-*" --json number,title
      9. Assert: at least one PR exists
    Expected Result: Task flows from Ready → Executing → Submitting → Done with a PR on GitHub
    Failure Indicators: Task stuck in any state for >20 minutes, no PR created, container crash
    Evidence: .sisyphus/evidence/task-7-e2e-flow.txt

  Scenario: Inngest Dev dashboard shows execution trace (MANUAL)
    Tool: Manual browser check (CANNOT BE AUTOMATED)
    Preconditions: E2E flow in progress or completed
    Steps:
      1. Open: http://localhost:8288 in browser
      2. Navigate to: Functions > engineering/task-lifecycle
      3. Verify: Recent run visible with all steps (update-status-executing, check-cancellation, dispatch-fly-machine, etc.)
    Expected Result: Full step trace visible in Inngest dashboard
    Failure Indicators: No function run visible, or steps show errors
    Evidence: .sisyphus/evidence/task-7-inngest-dashboard.png (screenshot)
    NOTE: This is the ONLY manual check in the entire Phase 8 plan. It cannot be automated because Inngest Dev Server has no programmatic API for inspecting function run history.
  ```

  **Evidence to Capture**:
  - [ ] `task-7-e2e-flow.txt` — webhook send, status polling, PR verification
  - [ ] `task-7-inngest-dashboard.png` — screenshot of Inngest Dev dashboard (manual)

  **Commit**: YES
  - Message: `test: phase 8 full e2e run — all 12 checks passing`
  - Files: Evidence files in `.sisyphus/evidence/`
  - Pre-commit: Task status = Done in Supabase

- [x] 8. Run Automated Verification + Capture Evidence

  **What to do**:
  - Run `scripts/verify-e2e.sh` against the completed E2E run from Task 7
  - Pass the task UUID from Task 7 as `--task-id` and the test repo as `--repo`
  - All 12 checks must pass (12/12)
  - Capture the full script output as evidence
  - If any check fails, diagnose and document the failure — do NOT modify source code (that would be a Phase 1-7 regression)
  - Also run `pnpm test -- --run` to confirm no test regressions (503+ tests, 0 failures)

  **Must NOT do**:
  - MUST NOT modify verify-e2e.sh to make checks pass (fix the system, not the test)
  - MUST NOT modify Phase 1-7 source code
  - MUST NOT skip failed checks

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Running the verification script and diagnosing any failures requires careful analysis
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: NO (sequential after T7)
  - **Parallel Group**: Wave 3
  - **Blocks**: Tasks 9, 10
  - **Blocked By**: Tasks 6, 7

  **References**:

  **Pattern References**:
  - `scripts/verify-e2e.sh` — The script created in Task 6. Run it.
  - `docs/2026-03-25-1901-mvp-implementation-phases.md:996-997` — "The MVP is functionally complete when ALL 12 checks pass."

  **WHY Each Reference Matters**:
  - The verification script IS the acceptance gate — its pass/fail output determines whether Phase 8 is complete

  **Acceptance Criteria**:
  - [ ] `scripts/verify-e2e.sh --task-id <uuid> --repo <owner/repo>` exits 0
  - [ ] Output shows "12/12 checks passed"
  - [ ] `pnpm test -- --run` shows 503+ tests, 0 failures
  - [ ] All evidence files saved to `.sisyphus/evidence/`

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: All 12 checks pass
    Tool: Bash
    Preconditions: E2E flow completed (Task 7), verify-e2e.sh exists (Task 6)
    Steps:
      1. Run: bash scripts/verify-e2e.sh --task-id <uuid> --repo <owner/repo> 2>&1 | tee .sisyphus/evidence/task-8-verify-output.txt
      2. Assert: exit code is 0
      3. Assert: output contains "12/12 checks passed"
      4. Run: pnpm test -- --run 2>&1 | tee .sisyphus/evidence/task-8-test-regression.txt
      5. Assert: 503+ tests, 0 failures
    Expected Result: Full verification passes with zero failures
    Failure Indicators: Any check showing "FAIL" or test regression
    Evidence: .sisyphus/evidence/task-8-verify-output.txt

  Scenario: Test regression check
    Tool: Bash
    Preconditions: Full E2E flow completed
    Steps:
      1. Run: pnpm test -- --run 2>&1 | tail -10
      2. Assert: test count >= 503
      3. Assert: 0 failures
    Expected Result: No tests broken by Phase 8 activities
    Failure Indicators: Test count dropped or failures introduced
    Evidence: .sisyphus/evidence/task-8-test-regression.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-8-verify-output.txt` — full verify-e2e.sh output
  - [ ] `task-8-test-regression.txt` — test suite summary

  **Commit**: YES (groups with T7 evidence)
  - Message: `test: phase 8 full e2e run — all 12 checks passing`
  - Files: `.sisyphus/evidence/task-8-*`
  - Pre-commit: `scripts/verify-e2e.sh` exits 0

- [x] 9. Update progress.json with Phase 8 Checkpoints

  **What to do**:
  - Edit `.sisyphus/progress.json` to update Phase 8 status and all 7 checkpoints
  - Set `phases[7].status` to `"complete"`
  - Set `phases[7].started_at` to the ISO timestamp when work began
  - Set `phases[7].completed_at` to the current ISO timestamp
  - Set `phases[7].plan_file` to `".sisyphus/plans/2026-04-01-0812-phase8-e2e.md"`
  - Set `phases[7].doc_file` to `"docs/YYYY-MM-DD-HHMM-phase8-e2e.md"` (use actual timestamp)
  - Update each checkpoint to `"status": "complete"` with `verified_at` timestamps and appropriate `verify_command` values
  - Update `last_updated` and `last_session_id` at the root level
  - Update `phases[7].resume_hint` to `"Phase 8 is complete. Proceed to Phase 9 (Cloud Deployment)."`

  **Must NOT do**:
  - MUST NOT add new checkpoints beyond the 7 already defined
  - MUST NOT modify any Phase 1-7 checkpoint data
  - MUST NOT change the JSON schema or add new root-level fields

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: JSON editing with known values
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: NO (sequential — needs T8 complete for verification timestamps)
  - **Parallel Group**: Wave 4
  - **Blocks**: Task 10
  - **Blocked By**: Task 8

  **References**:

  **Pattern References**:
  - `.sisyphus/progress.json` Phase 7 entry (lines 442-539) — Follow the EXACT same structure: same checkpoint format, same field names, same timestamp format

  **WHY Each Reference Matters**:
  - The progress.json format is established through 7 previous phases — consistency is mandatory

  **Acceptance Criteria**:
  - [ ] `phases[7].status` is `"complete"`
  - [ ] All 7 checkpoints (`all_services_running`, `webhook_to_pr_flow`, `full_status_audit`, `all_records_populated`, `container_cleanup`, `twelve_point_checklist`, `documented`) have `"status": "complete"`
  - [ ] `plan_file` and `doc_file` paths are correct
  - [ ] `last_updated` is recent
  - [ ] JSON is valid: `cat .sisyphus/progress.json | jq . > /dev/null`

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Progress.json is valid and complete
    Tool: Bash
    Preconditions: progress.json updated
    Steps:
      1. Run: cat .sisyphus/progress.json | jq '.phases[7].status'
      2. Assert: output is "complete"
      3. Run: cat .sisyphus/progress.json | jq '[.phases[7].checkpoints | to_entries[] | select(.value.status != "complete")] | length'
      4. Assert: output is 0 (all checkpoints complete)
      5. Run: cat .sisyphus/progress.json | jq '.phases[7].plan_file'
      6. Assert: output is ".sisyphus/plans/2026-04-01-0812-phase8-e2e.md"
      7. Run: cat .sisyphus/progress.json | jq . > /dev/null 2>&1
      8. Assert: exit code 0 (valid JSON)
    Expected Result: All Phase 8 fields correctly populated, JSON valid
    Failure Indicators: Any checkpoint not "complete", invalid JSON, wrong file paths
    Evidence: .sisyphus/evidence/task-9-progress-json.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-9-progress-json.txt` — jq query results

  **Commit**: YES
  - Message: `chore: update progress.json with phase 8 completion`
  - Files: `.sisyphus/progress.json`
  - Pre-commit: `cat .sisyphus/progress.json | jq . > /dev/null`

- [x] 10. Write Phase 8 Completion Documentation

  **What to do**:
  - Run `date "+%Y-%m-%d-%H%M"` to get the timestamp for the filename
  - Create `docs/YYYY-MM-DD-HHMM-phase8-e2e.md` following the EXACT structure of `docs/2026-04-01-0114-phase7-resilience.md`
  - Sections to include:
    - **What This Document Is** — One paragraph describing Phase 8's purpose
    - **What Was Built** — Mermaid diagram showing the E2E flow + numbered walkthrough table
    - **Project Structure** — Show new files added (scripts/, test-payloads/, test repo)
    - **E2E Flow Architecture** — Mermaid sequence diagram of the complete webhook-to-PR flow (adapted from §11 but showing actual local services)
    - **12-Point Verification Results** — Table showing each check, its command, and PASS/FAIL result
    - **Test Suite** — Summary table of evidence files and what each proves
    - **Key Design Decisions** — Why minimal test repo, why automated verification, why dev-start.sh
    - **Known Limitations** — List any issues discovered during E2E run
    - **What Phase 9 Builds On Top of This** — Brief overview + Mermaid diagram showing Phase 8 → Phase 9 transition
  - Target length: ~200-300 lines (Phase 8 has no new modules, so shorter than Phase 7's 484 lines)
  - All Mermaid diagrams must use the standard color palette (classDef from AGENTS.md)

  **Must NOT do**:
  - MUST NOT add sections not present in the Phase 7 doc template
  - MUST NOT invent new Mermaid color schemes
  - MUST NOT include credentials, tokens, or secrets in the doc
  - MUST NOT document Phase 1-7 work (only Phase 8 deliverables)

  **Recommended Agent Profile**:
  - **Category**: `writing`
    - Reason: Technical documentation with Mermaid diagrams following established conventions
  - **Skills**: [`v-mermaid`]
    - `v-mermaid`: Phase 8 doc requires 3-4 Mermaid diagrams following project conventions

  **Parallelization**:
  - **Can Run In Parallel**: NO (needs T8 and T9 for content)
  - **Parallel Group**: Wave 4 (sequential after T9)
  - **Blocks**: F1-F4
  - **Blocked By**: Tasks 8, 9

  **References**:

  **Pattern References**:
  - `docs/2026-04-01-0114-phase7-resilience.md` — The EXACT template to follow. Same section headings, same Mermaid conventions, same walkthrough table format.
  - `docs/2026-03-26-1511-phase1-foundation.md` — Alternative reference for completion doc structure (shorter, since Phase 1 was also foundational)

  **External References**:
  - AGENTS.md Mermaid color palette — `service: #4A90E2`, `storage: #7B68EE`, `external: #F5A623`, `future: #B0B0B0`, `event: #50C878`, `decision: #F8E71C`

  **WHY Each Reference Matters**:
  - Phase 7 doc is the most recent and most comprehensive template
  - Phase 1 doc shows how to document a foundational/testing phase (similar nature to Phase 8)
  - Mermaid palette must match for visual consistency across all docs

  **Acceptance Criteria**:
  - [ ] File exists at `docs/YYYY-MM-DD-HHMM-phase8-e2e.md` with correct timestamp
  - [ ] Contains all sections from the Phase 7 doc template
  - [ ] Contains at least 3 Mermaid diagrams using the standard palette
  - [ ] Contains the 12-point verification results table
  - [ ] Contains a "What Phase 9 Builds On Top of This" section
  - [ ] ~200-300 lines (appropriate for a verification phase)

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Doc follows Phase 7 template structure
    Tool: Bash
    Preconditions: Doc file created
    Steps:
      1. Run: grep -c "^## " docs/*phase8*.md
      2. Assert: at least 8 section headers (matching Phase 7 doc sections)
      3. Run: grep -c "classDef" docs/*phase8*.md
      4. Assert: at least 3 (one per Mermaid diagram)
      5. Run: grep -c "#4A90E2" docs/*phase8*.md
      6. Assert: at least 1 (using standard color palette)
      7. Run: wc -l docs/*phase8*.md
      8. Assert: line count between 150 and 400
    Expected Result: Doc has correct structure, diagrams, and length
    Failure Indicators: Missing sections, wrong colors, too short/long
    Evidence: .sisyphus/evidence/task-10-doc-structure.txt

  Scenario: Doc contains 12-point verification results
    Tool: Bash
    Preconditions: Doc file created
    Steps:
      1. Run: grep -c "PASS\|check" docs/*phase8*.md
      2. Assert: at least 12 (one per verification check)
    Expected Result: All 12 checks documented with their results
    Failure Indicators: Missing checks
    Evidence: .sisyphus/evidence/task-10-verification-table.txt
  ```

  **Evidence to Capture**:
  - [ ] `task-10-doc-structure.txt` — section count + diagram count + line count
  - [ ] `task-10-verification-table.txt` — grep for verification checks

  **Commit**: YES
  - Message: `docs: add phase 8 full local e2e completion documentation`
  - Files: `docs/YYYY-MM-DD-HHMM-phase8-e2e.md`
  - Pre-commit: File exists and has correct structure

---

## Final Verification Wave (MANDATORY — after ALL implementation tasks)

> 4 review agents run in PARALLEL. ALL must APPROVE. Present consolidated results to user and get explicit "okay" before completing.
>
> **Do NOT auto-proceed after verification. Wait for user's explicit approval before marking work complete.**

- [ ] F1. **Plan Compliance Audit** — `oracle`
      Read the plan end-to-end. For each "Must Have": verify implementation exists. For each "Must NOT Have": search codebase for forbidden patterns — reject with file:line if found. Check evidence files exist in `.sisyphus/evidence/`. Compare deliverables against plan.
      Output: `Must Have [N/N] | Must NOT Have [N/N] | Tasks [N/N] | VERDICT: APPROVE/REJECT`

- [ ] F2. **Code Quality Review** — `unspecified-high`
      Run `pnpm tsc --noEmit` + `pnpm lint` + `pnpm test -- --run`. Verify 503+ tests pass with 0 failures. Check all new scripts have `set -o pipefail`, proper error handling, and `chmod +x`. Check for hardcoded credentials or secrets in scripts. Verify no `console.log` in `src/gateway/`.
      Output: `Build [PASS/FAIL] | Lint [PASS/FAIL] | Tests [N pass/N fail] | Scripts [N clean/N issues] | VERDICT`

- [ ] F3. **Real Manual QA** — `unspecified-high`
      Start from clean state. Run `scripts/dev-start.sh`, wait for services healthy. Send realistic Jira webhook. Monitor the full E2E flow in real-time. Run `scripts/verify-e2e.sh` and confirm 12/12 pass. Test edge cases: send duplicate webhook (idempotency), send invalid payload (400). Save evidence to `.sisyphus/evidence/final-qa/`.
      Output: `Scenarios [N/N pass] | Integration [N/N] | Edge Cases [N tested] | VERDICT`

- [ ] F4. **Scope Fidelity Check** — `deep`
      For each task: read "What to do", read actual diff (`git log/diff`). Verify 1:1 match — everything in spec was built (no missing), nothing beyond spec was built (no creep). Check "Must NOT do" compliance. Detect cross-task contamination. Flag unaccounted changes.
      Output: `Tasks [N/N compliant] | Contamination [CLEAN/N issues] | Unaccounted [CLEAN/N files] | VERDICT`

---

## Commit Strategy

| #   | Commit                                                                         | Contents                                        | Pre-commit Check                                                |
| --- | ------------------------------------------------------------------------------ | ----------------------------------------------- | --------------------------------------------------------------- |
| 1   | `chore(gateway): confirm structured logging — no console.log migration needed` | Audit result (or trivial fix)                   | `pnpm test -- --run`                                            |
| 2   | `feat: create minimal test repo for e2e execution target`                      | GitHub repo (external) + seed update            | `pnpm prisma db seed`                                           |
| 3   | `test: add realistic jira webhook fixture for e2e testing`                     | `test-payloads/jira-realistic-task.json`        | `pnpm test -- --run`                                            |
| 4   | `feat: add dev startup and e2e verification scripts`                           | `scripts/dev-start.sh`, `scripts/verify-e2e.sh` | `bash -n scripts/dev-start.sh && bash -n scripts/verify-e2e.sh` |
| 5   | `test: phase 8 full e2e run — all 12 checks passing`                           | Evidence files only                             | `scripts/verify-e2e.sh` exits 0                                 |
| 6   | `chore: update progress.json with phase 8 completion`                          | `.sisyphus/progress.json`                       | `pnpm test -- --run`                                            |
| 7   | `docs: add phase 8 full local e2e completion documentation`                    | `docs/YYYY-MM-DD-HHMM-phase8-e2e.md`            | File exists, follows template                                   |

---

## Success Criteria

### Verification Commands

```bash
# 12-point E2E checklist
bash scripts/verify-e2e.sh
# Expected: 12/12 checks passed, exit 0

# Test regression
pnpm test -- --run
# Expected: 503+ tests, 0 failures

# Progress.json completeness
cat .sisyphus/progress.json | grep -c '"status": "complete"' | grep -q '8'
# Expected: Phase 8 has all checkpoints complete

# TypeScript clean
pnpm tsc --noEmit
# Expected: 0 errors
```

### Final Checklist

- [ ] All "Must Have" present
- [ ] All "Must NOT Have" absent
- [ ] All 12 E2E checks pass
- [ ] 503+ tests still passing
- [ ] Progress.json updated
- [ ] Phase 8 completion doc committed
