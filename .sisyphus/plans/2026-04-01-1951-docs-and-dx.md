# Documentation & Developer Experience Overhaul

## TL;DR

> **Quick Summary**: Fix database naming (`postgres` → `ai_employee`), add PostgREST migration for grants/UUID defaults, create root README, write zx TypeScript scripts (setup, trigger-task, dev-start, verify-e2e), and update AGENTS.md — making the project self-documenting and zero-friction to set up from scratch.
>
> **Deliverables**:
>
> - Prisma migration: PostgREST GRANT + UUID defaults for all tables
> - Database switch: `postgres` → `ai_employee` everywhere
> - Root `README.md` (≤150 lines, links to docs/)
> - `scripts/setup.ts` — interactive zx setup wizard
> - `scripts/trigger-task.ts` — send mock webhook + monitor
> - `scripts/dev-start.ts` — zx port of dev-start.sh
> - `scripts/verify-e2e.ts` — zx port of verify-e2e.sh
> - Updated `.env.example` with all env vars
> - Updated `AGENTS.md` with accurate project info
> - Troubleshooting runbook document
>
> **Estimated Effort**: Medium (2-3 sessions)
> **Parallel Execution**: YES — 4 waves
> **Critical Path**: T1 (zx dep) → T3 (migration) → T5 (setup script) → T8 (trigger script)

---

## Context

### Original Request

User identified 8 documentation gaps after completing Phase 8 E2E. All tribal knowledge from Phase 8 debugging (HMAC signing, PostgREST grants, OpenCode auth, database naming) is trapped in conversation history and `.sisyphus/notepads/`. The user wants a zero-friction developer experience: clone the repo, run a setup script, trigger a task, and have it work.

### Interview Summary

**Key Discussions**:

- DB name: User explicitly wants `ai_employee` (not `postgres`). `.env.example` already says `ai_employee` but actual `.env` uses `postgres`
- Scripts: User wants zx (TypeScript) for new scripts, NOT bash. Cited `https://github.com/google/zx` for testability + code quality
- Setup: User wants an interactive script that runs through all setup steps, not just a guide
- PostgREST: User wants the GRANT/UUID issue fixed via migration, not manual SQL
- Discoverability: User expects decisions that increase code quality and discoverability

**Research Findings**:

- PostgREST root cause: Prisma `@default(uuid())` is CLIENT-SIDE. PostgREST needs DB-SIDE `gen_random_uuid()`. GRANTs needed because `service_role` ≠ `postgres` owner
- Migration pattern exists: `20260326135326_add_check_constraints` already uses raw SQL
- zx 8.x compatible with project (Node 20+, ESM, tsx available)
- AGENTS.md audit: accurate on commit/markdown/Mermaid rules, wrong on DB convention, missing project-specific info
- 5 existing scripts: migrate dev-start.sh + verify-e2e.sh to zx; keep 3 others as bash

### Metis Review

**Identified Gaps** (addressed):

- Must verify if `ai_employee` DB already exists before writing setup script
- AGENTS.md is global (`~/.config/opencode/`) — changes affect all projects on machine
- Setup script must be idempotent (handle partial setup state)
- PostgREST migration needs `IF NOT EXISTS` guards
- zx must go in `devDependencies`, not `dependencies`
- Bash scripts must be preserved alongside zx versions until verified
- README must link to docs, not duplicate them (≤150 lines)
- Troubleshooting guide must only document evidenced failures, not invented ones

---

## Work Objectives

### Core Objective

Make the AI Employee platform self-documenting and zero-friction: a developer cloning the repo can run `scripts/setup.ts`, then `scripts/trigger-task.ts`, and see a complete Jira-to-PR flow within 15 minutes.

### Concrete Deliverables

- `prisma/migrations/<timestamp>_postgrest_grants/migration.sql` — UUID defaults + GRANT for all 16 tables
- `.env` and `.env.example` updated for `ai_employee` DB + 3 missing env vars
- `README.md` at project root (≤150 lines)
- `scripts/setup.ts` — interactive zx setup (idempotent)
- `scripts/trigger-task.ts` — send webhook + monitor to completion
- `scripts/dev-start.ts` — zx port of `dev-start.sh`
- `scripts/verify-e2e.ts` — zx port of `verify-e2e.sh`
- `docs/2026-04-01-HHMM-troubleshooting.md` — common failures + fixes
- Updated `~/.config/opencode/AGENTS.md`
- Updated `package.json` with zx dep + npm script entries

### Definition of Done

- [ ] `npx tsx scripts/setup.ts` runs on fresh clone → all services running, DB seeded
- [ ] `npx tsx scripts/trigger-task.ts` sends webhook → task reaches `Done` in ≤10 min
- [ ] `npx tsx scripts/verify-e2e.ts --task-id <uuid>` → 12/12 checks pass
- [ ] `curl http://localhost:54321/rest/v1/tasks?limit=1 -H "apikey: <key>"` → 200 (not 403)
- [ ] Test suite: 515+ tests passing (no regressions)

### Must Have

- All new scripts in TypeScript (zx)
- PostgREST migration with idempotent SQL (IF NOT EXISTS guards)
- Database named `ai_employee` everywhere
- Root README.md with quickstart
- Troubleshooting guide with ONLY evidenced failures

### Must NOT Have (Guardrails)

- MUST NOT modify any source code in `src/` — this is a DX-only pass
- MUST NOT modify existing Prisma schema (`.prisma` file) — only add a new migration
- MUST NOT delete bash scripts — zx scripts are additions until verified in a separate commit
- MUST NOT add `zx` to production `dependencies` — `devDependencies` only
- MUST NOT modify `Dockerfile` or `supabase/config.toml`
- MUST NOT add RLS policies in the PostgREST migration — GRANT only
- MUST NOT create OpenAPI specs, CHANGELOG, or CONTRIBUTING.md
- MUST NOT modify existing phase docs (1-8) — they are historical records
- MUST NOT modify `tests/` directory — no test changes in a DX pass
- MUST NOT invent troubleshooting scenarios — only document failures evidenced in git history or Phase 8 notepads

---

## Verification Strategy

> **ZERO HUMAN INTERVENTION** — ALL verification is agent-executed.

### Test Decision

- **Infrastructure exists**: YES (Vitest)
- **Automated tests**: NO — this is a DX/docs pass, not a code change. Verification is via script execution + curl checks
- **Framework**: Vitest (existing, but not touched in this plan)

### QA Policy

Every task MUST include agent-executed QA scenarios.
Evidence saved to `.sisyphus/evidence/task-{N}-{scenario-slug}.{ext}`.

- **Scripts**: Use Bash — run the script, check exit code, verify output
- **Migration**: Use Bash (psql/curl) — verify table grants, UUID defaults
- **Docs**: Use Bash (grep/wc) — verify line count, link targets exist

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Foundation — no dependencies):
├── T1: Add zx to devDependencies + package.json scripts [quick]
├── T2: Fix .env.example (add missing vars, fix DB name) [quick]
├── T3: Create PostgREST migration (GRANT + UUID defaults) [unspecified-high]
└── T4: Write root README.md [writing]

Wave 2 (Core scripts — depends on T1 for zx, T3 for DB):
├── T5: Create scripts/setup.ts (interactive setup wizard) [deep]
├── T6: Create scripts/trigger-task.ts (webhook + monitor) [deep]
└── T7: Write troubleshooting doc [writing]

Wave 3 (Script migrations — depends on T1 for zx):
├── T8: Migrate dev-start.sh → dev-start.ts (zx) [unspecified-high]
└── T9: Migrate verify-e2e.sh → verify-e2e.ts (zx) [unspecified-high]

Wave 4 (Final polish — depends on all above):
├── T10: Update AGENTS.md [quick]
├── T11: Update system-overview.md with new script references [quick]
└── T12: Switch local DB from postgres to ai_employee + verify full E2E [deep]

Wave FINAL (After ALL tasks — 4 parallel reviews, then user okay):
├── F1: Plan compliance audit (oracle)
├── F2: Code quality review (unspecified-high)
├── F3: Real manual QA (unspecified-high)
└── F4: Scope fidelity check (deep)
-> Present results -> Get explicit user okay

Critical Path: T1 → T3 → T5 → T12 → F1-F4 → user okay
Parallel Speedup: ~60% faster than sequential
Max Concurrent: 4 (Wave 1)
```

### Dependency Matrix

| Task | Depends On         | Blocks         |
| ---- | ------------------ | -------------- |
| T1   | —                  | T5, T6, T8, T9 |
| T2   | —                  | T5             |
| T3   | —                  | T5, T12        |
| T4   | —                  | T11            |
| T5   | T1, T2, T3         | T12            |
| T6   | T1                 | T12            |
| T7   | —                  | —              |
| T8   | T1                 | T12            |
| T9   | T1                 | T12            |
| T10  | —                  | —              |
| T11  | T4                 | —              |
| T12  | T3, T5, T6, T8, T9 | F1-F4          |

### Agent Dispatch Summary

- **Wave 1**: 4 tasks — T1 `quick`, T2 `quick`, T3 `unspecified-high`, T4 `writing`
- **Wave 2**: 3 tasks — T5 `deep`, T6 `deep`, T7 `writing`
- **Wave 3**: 2 tasks — T8 `unspecified-high`, T9 `unspecified-high`
- **Wave 4**: 3 tasks — T10 `quick`, T11 `quick`, T12 `deep`
- **FINAL**: 4 tasks — F1 `oracle`, F2 `unspecified-high`, F3 `unspecified-high`, F4 `deep`

---

## TODOs

- [x] 1. Add zx to devDependencies + package.json scripts

  **What to do**:
  - `pnpm add -D zx`
  - Add npm scripts to `package.json` for all planned zx scripts:
    ```json
    "setup": "tsx scripts/setup.ts",
    "trigger-task": "tsx scripts/trigger-task.ts",
    "dev:start": "tsx scripts/dev-start.ts",
    "verify:e2e": "tsx scripts/verify-e2e.ts"
    ```
  - Verify `node_modules/.bin/zx --version` returns 8.x

  **Must NOT do**:
  - MUST NOT add zx to production `dependencies`
  - MUST NOT modify any existing scripts

  **Recommended Agent Profile**:
  - **Category**: `quick`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with T2, T3, T4)
  - **Blocks**: T5, T6, T8, T9
  - **Blocked By**: None

  **References**:
  - `package.json` — current dependencies and scripts section
  - zx docs: `https://github.com/google/zx` — version 8.x API

  **Acceptance Criteria**:
  - [ ] `pnpm exec zx --version` returns 8.x
  - [ ] `package.json` has `zx` in `devDependencies`
  - [ ] `package.json` has 4 new script entries (setup, trigger-task, dev:start, verify:e2e)
  - [ ] `pnpm install` exits 0

  **QA Scenarios**:

  ```
  Scenario: zx is available after install
    Tool: Bash
    Steps:
      1. Run `pnpm exec zx --version`
      2. Assert output starts with "8."
    Expected Result: Version 8.x printed
    Evidence: .sisyphus/evidence/task-1-zx-version.txt
  ```

  **Commit**: YES
  - Message: `chore(deps): add zx to devDependencies`
  - Files: `package.json`, `pnpm-lock.yaml`

- [x] 2. Fix .env.example (add missing vars, fix DB name, mark required vs optional)

  **What to do**:
  - Read current `.env.example` (23 lines)
  - Verify `DATABASE_URL` already shows `ai_employee` (confirmed by research)
  - Add these 3 missing env vars:
    ```
    FLY_API_TOKEN="your-fly-api-token"
    FLY_WORKER_APP="ai-employee-workers"
    OPENROUTER_MODEL="minimax/minimax-m2.7"
    ```
  - Add `USE_LOCAL_DOCKER` and `INNGEST_BASE_URL` (used in lifecycle.ts but not in .env.example)
  - Group vars by phase/service with comments marking required vs optional
  - Ensure `DATABASE_URL` uses `ai_employee` (it already does in .env.example)

  **Must NOT do**:
  - MUST NOT add actual secret values
  - MUST NOT modify `.env` (only `.env.example`)

  **Recommended Agent Profile**:
  - **Category**: `quick`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with T1, T3, T4)
  - **Blocks**: T5
  - **Blocked By**: None

  **References**:
  - `.env.example` — current file (23 lines)
  - `.env` — actual env vars in use (for comparison, DO NOT commit)
  - `src/inngest/lifecycle.ts` — env vars used for Docker dispatch (USE*LOCAL_DOCKER, FLY*\*, OPENROUTER_MODEL)
  - `src/workers/entrypoint.sh` — env vars used inside container
  - `docs/2026-04-01-1726-system-overview.md` lines 343-388 — env var reference table

  **Acceptance Criteria**:
  - [ ] `.env.example` contains FLY_API_TOKEN, FLY_WORKER_APP, OPENROUTER_MODEL
  - [ ] `.env.example` contains USE_LOCAL_DOCKER, INNGEST_BASE_URL
  - [ ] All vars have descriptive comments marking required vs optional
  - [ ] DATABASE_URL uses `ai_employee` (not `postgres`)

  **QA Scenarios**:

  ```
  Scenario: All env vars used in code have an .env.example entry
    Tool: Bash
    Steps:
      1. grep for process.env.X in src/ and extract var names
      2. Check each appears in .env.example
    Expected Result: No missing vars (or documented exclusions)
    Evidence: .sisyphus/evidence/task-2-env-audit.txt
  ```

  **Commit**: YES
  - Message: `fix(env): update .env.example with missing vars and correct grouping`
  - Files: `.env.example`

- [x] 3. Create PostgREST migration (GRANT + UUID DB-side defaults)

  **What to do**:
  - Run `pnpm prisma migrate dev --create-only --name postgrest_grants` to create empty migration
  - Edit the generated SQL file to add:
    1. `ALTER TABLE <table> ALTER COLUMN id SET DEFAULT gen_random_uuid()` for ALL 16 tables
    2. `GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO anon, authenticated, service_role`
    3. `GRANT USAGE ON ALL SEQUENCES IN SCHEMA public TO anon, authenticated, service_role`
    4. `ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO anon, authenticated, service_role`
  - The `ALTER DEFAULT PRIVILEGES` ensures FUTURE tables also get grants automatically
  - Use `DO $$ BEGIN ... EXCEPTION WHEN ... END $$` blocks for idempotency where needed
  - Run `pnpm prisma migrate dev` to apply the migration
  - Verify via PostgREST: `curl http://localhost:54321/rest/v1/tasks?limit=1 -H "apikey: <key>"` returns 200

  **Must NOT do**:
  - MUST NOT modify `prisma/schema.prisma`
  - MUST NOT add RLS policies
  - MUST NOT add NOT NULL constraints to existing columns
  - MUST NOT backfill existing data

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with T1, T2, T4)
  - **Blocks**: T5, T12
  - **Blocked By**: None

  **References**:
  - `prisma/migrations/20260326135326_add_check_constraints/migration.sql` — pattern for raw SQL migrations
  - `prisma/schema.prisma` — all 16 table definitions (identify UUID columns)
  - `src/workers/lib/postgrest-client.ts` — how workers access Supabase (uses service_role key)
  - `src/workers/lib/completion.ts` — PATCH/POST calls that need grants

  **Acceptance Criteria**:
  - [ ] Migration file exists in `prisma/migrations/<ts>_postgrest_grants/`
  - [ ] `pnpm prisma migrate dev` exits 0
  - [ ] PostgREST GET returns 200 (not 403): `curl -sf http://localhost:54321/rest/v1/tasks?limit=1 -H "apikey: <key>"`
  - [ ] UUID default works: `psql -c "INSERT INTO tasks (external_id, source_system, status, tenant_id) VALUES ('test', 'jira', 'Ready', '00000000-0000-0000-0000-000000000001') RETURNING id;"` returns a UUID

  **QA Scenarios**:

  ```
  Scenario: PostgREST can read tasks table via service_role
    Tool: Bash
    Steps:
      1. Get SUPABASE_SECRET_KEY from .env
      2. curl -sf http://localhost:54321/rest/v1/tasks?limit=1 -H "apikey: $KEY" -H "Authorization: Bearer $KEY"
      3. Assert HTTP 200
    Expected Result: HTTP 200 with JSON array
    Evidence: .sisyphus/evidence/task-3-postgrest-read.txt

  Scenario: PostgREST can insert row with DB-generated UUID
    Tool: Bash
    Steps:
      1. curl -sf -X POST http://localhost:54321/rest/v1/tasks -H "apikey: $KEY" -H "Authorization: Bearer $KEY" -H "Content-Type: application/json" -H "Prefer: return=representation" -d '{"external_id":"uuid-test","source_system":"jira","status":"Ready","tenant_id":"00000000-0000-0000-0000-000000000001","project_id":"00000000-0000-0000-0000-000000000003"}'
      2. Assert response contains "id" field with UUID value
      3. Delete the test row
    Expected Result: JSON with auto-generated UUID id
    Evidence: .sisyphus/evidence/task-3-postgrest-insert.txt
  ```

  **Commit**: YES
  - Message: `feat(migration): add PostgREST grants and UUID DB-side defaults`
  - Files: `prisma/migrations/<ts>_postgrest_grants/migration.sql`

- [x] 4. Write root README.md

  **What to do**:
  - Create `README.md` at project root (≤150 lines)
  - Sections:
    1. **Title + one-line description**: "AI Employee Platform — automated Jira-to-PR workflow"
    2. **Quick Start** (5 steps): clone, `pnpm install`, `npx tsx scripts/setup.ts`, `npx tsx scripts/dev-start.ts`, `npx tsx scripts/trigger-task.ts`
    3. **Architecture Overview**: Brief paragraph + link to `docs/2026-04-01-1726-system-overview.md`
    4. **How It Works**: The 5-step flow (webhook → gateway → lifecycle → container → PR) in 5 bullet points
    5. **Project Structure**: Compact tree (src/gateway, src/inngest, src/workers, src/lib, scripts, prisma, docs)
    6. **Scripts Reference**: Table of all scripts with `npx tsx scripts/X.ts` usage
    7. **Environment Variables**: Link to system-overview.md env var section (don't duplicate)
    8. **Documentation**: List of all docs/ files with one-line descriptions
    9. **Troubleshooting**: Link to troubleshooting doc
    10. **Test Repo**: Note about `viiqswim/ai-employee-test-target` and its conventions (pnpm, script names)
  - Use standard Mermaid color palette for any diagrams
  - Every `docs/` link must resolve to an existing file

  **Must NOT do**:
  - MUST NOT exceed 150 lines
  - MUST NOT duplicate content from phase docs
  - MUST NOT include actual secret values
  - MUST NOT add badges, CI status, or npm version shields

  **Recommended Agent Profile**:
  - **Category**: `writing`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with T1, T2, T3)
  - **Blocks**: T11
  - **Blocked By**: None

  **References**:
  - `docs/2026-04-01-1726-system-overview.md` — comprehensive system reference (link to, don't duplicate)
  - `docs/2026-03-22-2317-readme.md` — existing docs index (will be superseded by root README)
  - All 12 docs files — for the documentation index section
  - `package.json` — for scripts section

  **Acceptance Criteria**:
  - [ ] `README.md` exists at project root
  - [ ] `wc -l README.md` returns ≤150
  - [ ] Every `docs/` link resolves: `grep -oE 'docs/[^)]+' README.md | xargs -I{} test -f {}`
  - [ ] Quick Start section has exactly 5 numbered steps

  **QA Scenarios**:

  ```
  Scenario: README link validation
    Tool: Bash
    Steps:
      1. Extract all doc links from README.md
      2. For each, verify file exists with `test -f`
    Expected Result: All links resolve
    Evidence: .sisyphus/evidence/task-4-readme-links.txt
  ```

  **Commit**: YES
  - Message: `docs: add root README with quickstart and architecture overview`
  - Files: `README.md`

- [x] 5. Create scripts/setup.ts (interactive zx setup wizard)

  **What to do**:
  - Create `scripts/setup.ts` using zx that walks through full from-scratch setup
  - Each step must be **idempotent** — check if already done before executing
  - Steps:
    1. Check prerequisites: Node ≥20, pnpm, Docker, supabase CLI, git
    2. Check if `.env` exists — if not, copy from `.env.example` and prompt user to fill in API keys
    3. Start Supabase (`supabase start`) — skip if already running (check port 54321)
    4. Create `ai_employee` database if it doesn't exist: `CREATE DATABASE ai_employee`
    5. Run Prisma migrations: `pnpm prisma migrate deploy`
    6. Run Prisma seed: `pnpm prisma db seed`
    7. Build Docker image: `docker build -t ai-employee-worker:latest .`
    8. Verify health: curl Supabase, check DB tables exist, check seed data
    9. Print summary of what's ready and what needs manual attention (API keys)
  - Use colored output (zx `chalk` is available)
  - Each step prints `✓ Step N: description` or `✗ Step N: error message`
  - Exit 0 if all steps pass, exit 1 with clear error message if any fail
  - WARN (not error) before destructive operations like `supabase db reset`

  **Must NOT do**:
  - MUST NOT run `supabase db reset` without explicit `--reset` flag
  - MUST NOT store or display secret values
  - MUST NOT modify `src/` files
  - MUST NOT assume a clean state — handle partial setup gracefully

  **Recommended Agent Profile**:
  - **Category**: `deep`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with T6, T7)
  - **Blocks**: T12
  - **Blocked By**: T1 (zx dep), T2 (.env.example), T3 (migration)

  **References**:
  - `scripts/dev-start.sh` lines 1-50 — prerequisite checking pattern (adapt to TypeScript)
  - `prisma/seed.ts` — what seed data exists
  - `.sisyphus/notepads/phase8-e2e/learnings.md` — PostgREST grant commands that were run manually
  - zx docs: `$` for shell commands, `chalk` for colors, `question()` for prompts

  **Acceptance Criteria**:
  - [ ] `npx tsx scripts/setup.ts` exits 0 on first run (assuming prerequisites met)
  - [ ] `npx tsx scripts/setup.ts` exits 0 on second run (idempotent)
  - [ ] Script detects missing prerequisites and names them specifically
  - [ ] Script does NOT run `supabase db reset` without `--reset` flag

  **QA Scenarios**:

  ```
  Scenario: Setup script is idempotent
    Tool: Bash
    Steps:
      1. Run `npx tsx scripts/setup.ts`
      2. Verify exit code 0
      3. Run `npx tsx scripts/setup.ts` again
      4. Verify exit code 0 (no errors on re-run)
    Expected Result: Both runs exit 0
    Evidence: .sisyphus/evidence/task-5-setup-idempotent.txt

  Scenario: Setup script detects missing prerequisite
    Tool: Bash
    Steps:
      1. Temporarily rename `supabase` binary (or mock PATH)
      2. Run `npx tsx scripts/setup.ts`
      3. Assert output contains "supabase" in error message
      4. Assert exit code 1
    Expected Result: Clear error message naming missing tool
    Evidence: .sisyphus/evidence/task-5-setup-prereq-check.txt
  ```

  **Commit**: YES
  - Message: `feat(scripts): add interactive zx setup script`
  - Files: `scripts/setup.ts`, `package.json`

- [x] 6. Create scripts/trigger-task.ts (webhook + monitor)

  **What to do**:
  - Create `scripts/trigger-task.ts` using zx that:
    1. Accepts `--payload <path>` (default: `test-payloads/jira-realistic-task.json`)
    2. Accepts `--key <issue-key>` to override the issue key (generates unique key like `TEST-{timestamp}`)
    3. Reads `JIRA_WEBHOOK_SECRET` from `.env`
    4. Computes HMAC-SHA256 signature using Node.js `crypto` (not shell openssl)
    5. Sends `POST /webhooks/jira` with `--data-binary` equivalent (raw body)
    6. Prints task UUID from response
    7. Enters monitoring loop: polls DB every 30s, prints status + stage transitions
    8. Exits when task reaches `Done`, `AwaitingInput`, `Failed`, or `Cancelled`
    9. On `Done`: prints PR URL from deliverables table, exit 0
    10. On terminal error: prints failure reason, exit 1
  - Use `node:crypto` for HMAC (not shell commands)
  - Use `node:fs` for reading payload (not `$\`cat ...\``)
  - The script itself IS the documentation — add JSDoc comments explaining the webhook format

  **Must NOT do**:
  - MUST NOT use shell `openssl` for HMAC — use Node.js crypto module
  - MUST NOT hardcode the webhook secret
  - MUST NOT wait more than 20 minutes (timeout with clear message)
  - MUST NOT modify any payload files

  **Recommended Agent Profile**:
  - **Category**: `deep`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with T5, T7)
  - **Blocks**: T12
  - **Blocked By**: T1 (zx dep)

  **References**:
  - `src/gateway/validation/signature.ts` — how gateway verifies HMAC (match this exactly)
  - `src/gateway/routes/jira.ts` — what webhook payload fields are required
  - `src/gateway/validation/schemas.ts` — Zod schema for Jira webhook (exact required fields)
  - `test-payloads/jira-realistic-task.json` — example payload
  - Phase 8 E2E curl command (from conversation): `--data-binary` is required, not `-d`

  **Acceptance Criteria**:
  - [ ] `npx tsx scripts/trigger-task.ts` exits 0 when services are running
  - [ ] Gateway returns HTTP 200 (not 401) — HMAC matches
  - [ ] Script prints task UUID after webhook accepted
  - [ ] Monitoring loop shows status transitions
  - [ ] Script reports PR URL on completion

  **QA Scenarios**:

  ```
  Scenario: Trigger task with default payload (happy path)
    Tool: Bash
    Preconditions: Gateway running on :3000, Inngest on :8288, Supabase on :54322
    Steps:
      1. npx tsx scripts/trigger-task.ts --key TEST-$(date +%s)
      2. Wait for exit
    Expected Result: Exit 0, output contains "Task ID:", "Status: Done", and "PR:" URL
    Evidence: .sisyphus/evidence/task-6-trigger-happy.txt

  Scenario: Trigger task with invalid secret shows clear error
    Tool: Bash
    Steps:
      1. JIRA_WEBHOOK_SECRET=wrong npx tsx scripts/trigger-task.ts
      2. Assert output contains "401" or "signature"
    Expected Result: Clear error about signature mismatch
    Evidence: .sisyphus/evidence/task-6-trigger-bad-hmac.txt
  ```

  **Commit**: YES
  - Message: `feat(scripts): add trigger-task zx script for mock webhooks`
  - Files: `scripts/trigger-task.ts`, `package.json`

- [x] 7. Write troubleshooting guide

  **What to do**:
  - Create `docs/<timestamp>-troubleshooting.md`
  - Run `date "+%Y-%m-%d-%H%M"` for the filename timestamp
  - ONLY include failures that are evidenced in:
    - `.sisyphus/notepads/phase8-e2e/learnings.md` (467 lines of real debugging notes)
    - Git history (commit messages describing fixes)
    - Existing scripts (error handling paths)
  - Sections:
    1. **Gateway returns 401 on webhook** — HMAC uses raw body (`--data-binary`), not parsed JSON (`-d`)
    2. **Task stays in Ready forever** — Inngest Dev Server not running, or gateway not registered at `/api/inngest`
    3. **Container exits immediately with "REPO_URL not set"** — lifecycle.ts wasn't passing repoUrl in event (fixed in Phase 8, commit b919931)
    4. **OpenCode session returns empty `{}`** — auth.json not written before server starts, or wrong provider ID
    5. **PostgREST returns 403** — GRANT statements not applied (run migration T3)
    6. **Tests fail with FK errors after E2E run** — E2E run creates extra `agent_versions` row; re-seed with `pnpm prisma db seed`
    7. **Container takes >10 min and gets reset** — Watchdog stale detection; in local Docker mode, watchdog's Fly.io check fails silently so this shouldn't happen, but if it does, check `dispatch_attempts`
    8. **Branch already exists on GitHub** — Use a unique issue key (`TEST-{timestamp}`) to avoid conflicts
  - Each failure: Symptom → Root Cause → Fix (one-liner command)

  **Must NOT do**:
  - MUST NOT invent scenarios not evidenced in git/notepads
  - MUST NOT exceed 100 lines
  - MUST NOT duplicate architecture docs

  **Recommended Agent Profile**:
  - **Category**: `writing`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with T5, T6)
  - **Blocks**: None
  - **Blocked By**: None

  **References**:
  - `.sisyphus/notepads/phase8-e2e/learnings.md` — 467 lines of real debugging notes (PRIMARY SOURCE)
  - Git log: commits `11cc6e5`, `b919931`, `a4b65f7`, `62d584d`, `ff6ef19` — all Phase 8 bug fixes with descriptions
  - `scripts/verify-e2e.sh` — error handling paths show what can go wrong

  **Acceptance Criteria**:
  - [ ] File exists at `docs/<ts>-troubleshooting.md`
  - [ ] Contains ≥ 6 failure scenarios
  - [ ] Each scenario has Symptom, Root Cause, and Fix sections
  - [ ] Every fix command is copy-pasteable

  **QA Scenarios**:

  ```
  Scenario: Every fix command in troubleshooting doc is syntactically valid
    Tool: Bash
    Steps:
      1. Extract code blocks from doc
      2. bash -n each (syntax check only, don't execute)
    Expected Result: No syntax errors
    Evidence: .sisyphus/evidence/task-7-troubleshoot-syntax.txt
  ```

  **Commit**: YES
  - Message: `docs: add troubleshooting guide for common E2E failures`
  - Files: `docs/<ts>-troubleshooting.md`

- [x] 8. Migrate dev-start.sh → dev-start.ts (zx)

  **What to do**:
  - Create `scripts/dev-start.ts` that replicates ALL functionality of `scripts/dev-start.sh` (235 lines)
  - Key features to preserve:
    1. `--reset` flag for database reset
    2. Prerequisite checks (docker, supabase, pnpm, inngest-cli)
    3. Cleanup trap (SIGINT/SIGTERM → kill child processes)
    4. Health polling with retry (Supabase, Inngest, Gateway)
    5. Env loading from `.env`
    6. Process management (start/stop Inngest, Gateway as child processes)
    7. `USE_LOCAL_DOCKER=1` set for gateway
    8. Color-coded output
  - Use zx `$` for shell commands, `process.on('SIGINT', ...)` for cleanup
  - Use `chalk` for colored output
  - Import `dotenv` or use `node --env-file=.env` pattern for env loading
  - Keep `scripts/dev-start.sh` intact (don't delete)
  - Add `"dev:start": "tsx scripts/dev-start.ts"` to package.json scripts

  **Must NOT do**:
  - MUST NOT delete `scripts/dev-start.sh`
  - MUST NOT change behavior vs bash version
  - MUST NOT start services that are already running (idempotent)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 3 (with T9)
  - **Blocks**: T12
  - **Blocked By**: T1 (zx dep)

  **References**:
  - `scripts/dev-start.sh` — the bash original (235 lines, read EVERY line before writing TypeScript)
  - `package.json` — scripts section for new entry

  **Acceptance Criteria**:
  - [ ] `npx tsx scripts/dev-start.ts` starts all services (exit 0 when all healthy)
  - [ ] `npx tsx scripts/dev-start.ts --reset` resets DB before starting
  - [ ] Ctrl+C cleanly kills all child processes
  - [ ] `curl http://localhost:3000/health` returns 200 after script completes

  **QA Scenarios**:

  ```
  Scenario: dev-start.ts starts all services
    Tool: Bash
    Steps:
      1. Kill any running services first
      2. npx tsx scripts/dev-start.ts &
      3. Wait 30s
      4. curl http://localhost:3000/health
      5. curl http://localhost:8288/
      6. curl http://localhost:54321/health
      7. Kill the script
    Expected Result: All 3 health checks return 200
    Evidence: .sisyphus/evidence/task-8-devstart-health.txt
  ```

  **Commit**: YES
  - Message: `feat(scripts): migrate dev-start.sh to zx TypeScript`
  - Files: `scripts/dev-start.ts`, `package.json`

- [x] 9. Migrate verify-e2e.sh → verify-e2e.ts (zx)

  **What to do**:
  - Create `scripts/verify-e2e.ts` that replicates ALL 12 checks from `scripts/verify-e2e.sh` (229 lines)
  - Key features to preserve:
    1. `--task-id <uuid>` and `--repo <owner/repo>` arguments
    2. 12-point checklist with pass/fail counting
    3. Database queries via psql (task status, executions, deliverables, validation_runs, status_log)
    4. GitHub API checks via `gh` CLI
    5. Docker container inspection
    6. Polling loop for container cleanup (Check 12)
    7. Banner output with final score
  - Use zx `$` for external commands (psql, gh, docker)
  - Use `minimist` for argument parsing (bundled with zx)
  - Type-safe result tracking with interfaces
  - Keep `scripts/verify-e2e.sh` intact (don't delete)

  **Must NOT do**:
  - MUST NOT delete `scripts/verify-e2e.sh`
  - MUST NOT skip any of the 12 checks
  - MUST NOT change the check logic or pass/fail criteria

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 3 (with T8)
  - **Blocks**: T12
  - **Blocked By**: T1 (zx dep)

  **References**:
  - `scripts/verify-e2e.sh` — the bash original (229 lines, read EVERY line before writing TypeScript)
  - `package.json` — scripts section

  **Acceptance Criteria**:
  - [ ] `npx tsx scripts/verify-e2e.ts --task-id <uuid> --repo viiqswim/ai-employee-test-target` produces identical results to bash version
  - [ ] All 12 checks present and functional
  - [ ] Pass/fail counter matches bash version output format
  - [ ] Exit code 0 on all-pass, 1 on any failure

  **QA Scenarios**:

  ```
  Scenario: verify-e2e.ts matches bash version output
    Tool: Bash
    Preconditions: A Done task exists in DB
    Steps:
      1. Get latest Done task ID from DB
      2. Run bash version, capture output
      3. Run TypeScript version, capture output
      4. Compare check results (both should show same pass/fail)
    Expected Result: Same 12/12 results
    Evidence: .sisyphus/evidence/task-9-verify-parity.txt
  ```

  **Commit**: YES
  - Message: `feat(scripts): migrate verify-e2e.sh to zx TypeScript`
  - Files: `scripts/verify-e2e.ts`, `package.json`

- [x] 10. Update AGENTS.md

  **What to do**:
  - File: `~/.config/opencode/AGENTS.md` (GLOBAL file, not repo-local)
  - Updates:
    1. Fix the DB naming convention section: confirm `ai_employee` as the correct database name (matches the fix in T3/T12)
    2. Add an `ai-employee` project-specific section with:
       - Database: `ai_employee` on local Supabase port 54322
       - Test command: `pnpm test` (Vitest, 515+ tests, 2 known failures)
       - E2E: `npx tsx scripts/trigger-task.ts` to run full flow
       - Docker: `docker build -t ai-employee-worker:latest .` before E2E
       - Known test failures: container-boot.test.ts (needs Docker socket), inngest-serve.test.ts (function count)
    3. Verify Mermaid color palette is still accurate (confirmed by audit: it is)
    4. Do NOT change commit rules, markdown naming, or Mermaid rules — they are accurate

  **Must NOT do**:
  - MUST NOT change rules that are already accurate
  - MUST NOT add project-specific info that belongs in README.md (keep AGENTS.md concise)
  - MUST NOT remove the general DB naming convention — only add project-specific addendum

  **Recommended Agent Profile**:
  - **Category**: `quick`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 4 (with T11, T12)
  - **Blocks**: None
  - **Blocked By**: None

  **References**:
  - `~/.config/opencode/AGENTS.md` — current file (read before editing)
  - AGENTS.md audit results — accurate/wrong/missing findings
  - `README.md` (written in T4) — ensure no duplication

  **Acceptance Criteria**:
  - [ ] AGENTS.md DB convention section mentions `ai_employee` as the database name
  - [ ] AGENTS.md has an ai-employee project section with test/E2E/Docker commands
  - [ ] No contradictions between AGENTS.md and README.md

  **QA Scenarios**:

  ```
  Scenario: AGENTS.md and README agree on DB name
    Tool: Bash
    Steps:
      1. grep "ai_employee" ~/.config/opencode/AGENTS.md
      2. grep "ai_employee" README.md
      3. Neither should say "postgres" as the active database
    Expected Result: Both reference ai_employee
    Evidence: .sisyphus/evidence/task-10-agents-consistency.txt
  ```

  **Commit**: NO (global file, not in repo)

- [x] 11. Update system-overview.md with new script references

  **What to do**:
  - Update `docs/2026-04-01-1726-system-overview.md` section "Local Development Environment" to reference the new zx scripts instead of (or in addition to) the bash versions
  - Update "Database" section to reference `ai_employee` instead of `postgres`
  - Add references to `scripts/trigger-task.ts` and `scripts/setup.ts` in the appropriate sections
  - Keep it brief — just update references, don't rewrite sections

  **Must NOT do**:
  - MUST NOT rewrite the document
  - MUST NOT change sections unrelated to scripts/DB

  **Recommended Agent Profile**:
  - **Category**: `quick`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 4 (with T10, T12)
  - **Blocks**: None
  - **Blocked By**: T4 (README)

  **References**:
  - `docs/2026-04-01-1726-system-overview.md` — current file

  **Acceptance Criteria**:
  - [ ] system-overview.md references `scripts/setup.ts` and `scripts/trigger-task.ts`
  - [ ] system-overview.md says `ai_employee` (not `postgres`) for database name
  - [ ] No broken internal references

  **Commit**: YES
  - Message: `docs: update system-overview with new script references and ai_employee DB`
  - Files: `docs/2026-04-01-1726-system-overview.md`

- [x] 12. Switch local DB from postgres to ai_employee + verify full E2E

  **What to do**:
  - This is the capstone task that proves everything works together
  - Steps:
    1. Update `.env`: change `DATABASE_URL` and `DATABASE_URL_DIRECT` from `postgres` to `ai_employee`
    2. Create `ai_employee` database if not exists: `psql -c "CREATE DATABASE ai_employee"`
    3. Run migrations: `pnpm prisma migrate deploy`
    4. Run seed: `pnpm prisma db seed`
    5. Verify PostgREST works: curl to `/rest/v1/tasks` returns 200
    6. Run test suite: `pnpm test -- --run` — expect 515+ passing
    7. Start services via `npx tsx scripts/dev-start.ts`
    8. Build Docker image: `docker build -t ai-employee-worker:latest .`
    9. Run `npx tsx scripts/trigger-task.ts --key TEST-final`
    10. Wait for task to reach Done
    11. Run `npx tsx scripts/verify-e2e.ts --task-id <uuid> --repo viiqswim/ai-employee-test-target`
    12. All 12 checks must pass
  - This task validates the entire plan's deliverables end-to-end

  **Must NOT do**:
  - MUST NOT commit `.env` to git
  - MUST NOT wait more than 20 minutes for E2E
  - MUST NOT modify source code

  **Recommended Agent Profile**:
  - **Category**: `deep`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 4 (sequential — depends on all prior tasks)
  - **Blocks**: F1-F4
  - **Blocked By**: T3, T5, T6, T8, T9

  **References**:
  - All scripts created in T5, T6, T8, T9
  - Migration from T3
  - `.env` — update DB URLs

  **Acceptance Criteria**:
  - [ ] `.env` uses `ai_employee` database
  - [ ] Test suite passes (515+)
  - [ ] Full E2E: webhook → task Done → PR created
  - [ ] verify-e2e.ts reports 12/12 checks pass
  - [ ] No regressions from the database switch

  **QA Scenarios**:

  ```
  Scenario: Full E2E on ai_employee database
    Tool: Bash
    Steps:
      1. Verify .env shows ai_employee
      2. npx tsx scripts/trigger-task.ts --key TEST-final-$(date +%s)
      3. Wait for Done
      4. npx tsx scripts/verify-e2e.ts --task-id $TASK_ID --repo viiqswim/ai-employee-test-target
    Expected Result: 12/12 checks pass
    Evidence: .sisyphus/evidence/task-12-e2e-ai-employee.txt

  Scenario: Test suite passes on ai_employee DB
    Tool: Bash
    Steps:
      1. pnpm prisma db seed
      2. CI=true pnpm test -- --run
    Expected Result: 515+ tests passing
    Evidence: .sisyphus/evidence/task-12-test-regression.txt
  ```

  **Commit**: NO (`.env` is gitignored)

---

## Final Verification Wave

> 4 review agents run in PARALLEL. ALL must APPROVE. Present consolidated results to user and get explicit "okay" before completing.

- [x] F1. **Plan Compliance Audit** — `oracle`
      Read the plan end-to-end. For each "Must Have": verify implementation exists. For each "Must NOT Have": search codebase for forbidden patterns. Check evidence files exist. Compare deliverables against plan.
      Output: `Must Have [N/N] | Must NOT Have [N/N] | Tasks [N/N] | VERDICT: APPROVE/REJECT`

- [x] F2. **Code Quality Review** — `unspecified-high`
      Run `pnpm tsc --noEmit` + linter + `pnpm test -- --run`. Verify all zx scripts have proper error handling. Check for `as any`, empty catches, console.log in new TypeScript files.
      Output: `Build [PASS/FAIL] | Lint [PASS/FAIL] | Tests [N pass/N fail] | Scripts [N clean/N issues] | VERDICT`

- [x] F3. **Real Manual QA** — `unspecified-high`
      Run `scripts/setup.ts` on current state. Run `scripts/trigger-task.ts` and verify task reaches Done. Run `scripts/verify-e2e.ts` with the task ID. Check every link in README.md resolves.
      Output: `Scenarios [N/N pass] | Integration [N/N] | Edge Cases [N tested] | VERDICT`

- [x] F4. **Scope Fidelity Check** — `deep`
      For each task: read spec, read actual diff. Verify 1:1 match. Check "Must NOT" compliance. Detect cross-task contamination.
      Output: `Tasks [N/N compliant] | Contamination [CLEAN/N issues] | Unaccounted [CLEAN/N files] | VERDICT`

---

## Commit Strategy

| #   | Commit Message                                                        | Files                                                   |
| --- | --------------------------------------------------------------------- | ------------------------------------------------------- |
| 1   | `chore(deps): add zx to devDependencies`                              | `package.json`, `pnpm-lock.yaml`                        |
| 2   | `fix(env): update .env.example with missing vars and correct DB name` | `.env.example`                                          |
| 3   | `feat(migration): add PostgREST grants and UUID DB-side defaults`     | `prisma/migrations/<ts>_postgrest_grants/migration.sql` |
| 4   | `docs: add root README with quickstart and architecture overview`     | `README.md`                                             |
| 5   | `feat(scripts): add interactive zx setup script`                      | `scripts/setup.ts`, `package.json`                      |
| 6   | `feat(scripts): add trigger-task zx script for mock webhooks`         | `scripts/trigger-task.ts`, `package.json`               |
| 7   | `docs: add troubleshooting guide for common E2E failures`             | `docs/<ts>-troubleshooting.md`                          |
| 8   | `feat(scripts): migrate dev-start.sh to zx TypeScript`                | `scripts/dev-start.ts`, `package.json`                  |
| 9   | `feat(scripts): migrate verify-e2e.sh to zx TypeScript`               | `scripts/verify-e2e.ts`, `package.json`                 |
| 10  | `chore(agents): update AGENTS.md DB convention and project info`      | `~/.config/opencode/AGENTS.md`                          |
| 11  | `docs: update system-overview with new script references`             | `docs/2026-04-01-1726-system-overview.md`               |
| 12  | `fix(db): switch from postgres to ai_employee database`               | `.env`                                                  |

---

## Success Criteria

### Verification Commands

```bash
# PostgREST grants work
curl -sf http://localhost:54321/rest/v1/tasks?limit=1 -H "apikey: $(grep SUPABASE_SECRET_KEY .env | cut -d= -f2-)" | head -5

# UUID defaults work (DB-side)
PGPASSWORD=postgres psql -h localhost -p 54322 -U postgres -d ai_employee -c "INSERT INTO tasks (external_id, source_system, status, tenant_id) VALUES ('test-uuid', 'jira', 'Ready', '00000000-0000-0000-0000-000000000001') RETURNING id;"

# Setup script is idempotent
npx tsx scripts/setup.ts && npx tsx scripts/setup.ts  # Both exit 0

# Trigger script works
npx tsx scripts/trigger-task.ts --payload test-payloads/jira-realistic-task.json

# Test suite stable
pnpm prisma db seed && CI=true pnpm test -- --run  # 515+ passing

# README links valid
grep -oE '\(docs/[^)]+\)' README.md | tr -d '()' | xargs -I{} test -f {} && echo "All links valid"
```

### Final Checklist

- [ ] All "Must Have" present
- [ ] All "Must NOT Have" absent
- [ ] All tests pass (515+)
- [ ] Database is `ai_employee` (not `postgres`)
- [ ] PostgREST returns 200 (not 403)
- [ ] All zx scripts exit 0 on happy path
