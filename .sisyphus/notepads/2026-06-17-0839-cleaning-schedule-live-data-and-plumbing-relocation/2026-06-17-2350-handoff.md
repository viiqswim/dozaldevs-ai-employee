# Handoff — 2026-06-17 23:50

## TL;DR

All 4 user-requested priorities are **done, committed, and live-verified**. The remaining plan tasks (T15, F1–F4, T17) are **gated on an explicit user decision** — not on capability. A clean stop has been reached.

## Commits this session (6 source/doc + 1 chore)

| SHA        | What                                                                               |
| ---------- | ---------------------------------------------------------------------------------- |
| `30ef3020` | Strip plumbing from generated `delivery_steps` (the original user-reported bug)    |
| `349ba84b` | Auto-attach `/tools/composio/execute.ts` when steps reference a connected app      |
| `8842254f` | Align archetype-generator tests; full suite green                                  |
| `169add98` | Extract `ARCHETYPE_AUTHORING_RULES` shared constant (dedup both prompt paths)      |
| `4923a7b9` | Record `.sisyphus` artifacts                                                       |
| `8a41206b` | Docs: creating-archetypes skill (composio auto-attach + shared constant)           |
| `bd9cdb97` | Make capacity-overflow→backup rule defensive + unconditional (partial — see below) |

## Live E2E proof (user's explicit ask)

- Fresh employee from a SIMPLE description → archetype `26e179ab…` (since soft-deleted), task `06cfb30a-164e-49cf-9362-0f2c984804b0`, date `2026-06-20`.
- Reached **Done**. Env vars correct. `{{target_date}}` substituted to literal `2026-06-20` in compiled AGENTS.md. Composio tool auto-attached. Zero plumbing leaks.
- **Coverage: 11/11 Hostfully-checkout properties present in the posted Slack schedule** (verified by listing code AND propertyUid).

## Final Wave

- **F1 (plan compliance): APPROVE** · **F2 (code quality + regression): APPROVE** · **F4 (scope + no-leak): APPROVE**
- F1/F2/F4 are NOT marked `[x]` in the plan — plan line 872 forbids checking them before explicit user "okay".

## The one open defect (well-characterized)

**Backup-cleaner over-capacity reassignment.** In the run, the employee correctly _flagged_ Yessica's 460 min > 240 min Saturday cap and named backups, but did NOT move the overflow properties under the backup in the output. Coverage is unaffected (all 11 listed); this is overflow-routing correctness.

- Root cause: the capacity step was conditionally gated → omitted entirely with a simple description.
- `bd9cdb97` strengthened the rule to be defensive/unconditional, but a fresh generation still didn't reliably emit the capacity step → this is **model compliance**, needing iteration.
- **Recommended next step**: deterministic `postProcess` step-injection in `src/gateway/services/archetype-generator.ts` (mirror the composio injection at lines ~421-435), BUT it must insert the capacity step at the correct position (after assignment, before compile) and be validated with the 5-date replay so it doesn't regress roster employees platform-wide. That validation = the deferred F3/T15 loop.

## To resume — user picks one

- **(a) Approve the wave** → mark F1/F2/F4 `[x]`, run T17 Telegram (`tsx scripts/telegram-notify.ts "..."`).
- **(b) Greenlight the capacity loop** → implement the validated `postProcess` capacity-step injection + run T15's 5-date proof (oracles at `.sisyphus/artifacts2/oracle/{date}/`).
- **(c) Stop** → everything above stays committed and verified.

## Key gotchas (for the resumer)

- Local `.env` Slack tokens are EXPIRED. To read Slack from scripts, decrypt the tenant `slack_bot_token` (`tenant_secrets` cols: `ciphertext/iv/auth_tag`) via `src/lib/encryption.decrypt` + `ENCRYPTION_KEY`.
- The schedule is posted as a THREAD REPLY under the "✅ Task complete" notification — use `conversations.replies`, not the notify ts.
- Worker/delivery container logs TRUNCATE message bodies — fetch the schedule from Slack for ground truth.
- `vm_size='performance-1x'` is MANDATORY for opencode runtime (OOM otherwise).
- The refactor + all fixes are gateway-only — NO Docker rebuild needed.
- `admin-archetype-generate.test.ts` has a known intermittent mocked-DB flake (1 fail ~1 in 4 runs); not a real failure.
