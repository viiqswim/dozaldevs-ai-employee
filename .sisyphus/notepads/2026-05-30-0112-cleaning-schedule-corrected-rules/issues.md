# Issues

## [2026-05-30T06:23:47Z] Session Start

No issues yet. Monitor for:

- `default.json` deletion breaking any references
- Mock mode fixture flattening (nested `has_children` blocks must be top-level)
- SQL UPDATE applying correctly to both `execution_steps` and `identity`
