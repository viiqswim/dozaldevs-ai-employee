# Issues — local-guest-messaging-e2e

## [2026-05-01] Pre-existing LSP errors (NOT regressions)

- `scripts/verify-multi-tenancy.ts` has pre-existing TypeScript errors related to `slack_team_id` property
- These are NOT introduced by this plan — do not attempt to fix them
