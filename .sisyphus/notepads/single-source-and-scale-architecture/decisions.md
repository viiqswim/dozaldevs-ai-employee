# Decisions

## [2026-06-13] Architecture Decisions (Oracle-reviewed, finalized)

- **Generate, don't sync-test** World-B constants
- **Typed ToolDescriptor** (highest leverage — T2 early, own wave)
- **Startup-cached discovery** replaces request-time disk reads
- **Capability enforcement** of tool_registry — flag-gated, default OFF, T8 prerequisite
- **Output-contract versioning** — additive, backward-compatible, graceful degradation
- Backlog (NOT this plan): AGENTS.md typed-section schema; prompts as versioned template objects
